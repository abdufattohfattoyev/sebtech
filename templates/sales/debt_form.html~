{% extends 'base.html' %}
{% load static %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
<div class="container-fluid">
    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">{{ title }}</h3>
                </div>
                <div class="card-body">
                    <form method="post" id="debtForm">
                        {% csrf_token %}

                        {% if form.non_field_errors %}
                            <div class="alert alert-danger">
                                {% for error in form.non_field_errors %}
                                    {{ error }}
                                {% endfor %}
                            </div>
                        {% endif %}

                        <!-- Qarz turi (ko'rinmaydi, lekin formada bor) -->
                        <div style="display: none;">
                            {{ form.debt_type }}
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="{{ form.creditor.id_for_label }}">
                                        <i class="fas fa-user-tie"></i> Kimdan qarz olyapsiz? <span class="text-danger">*</span>
                                    </label>
                                    {{ form.creditor }}
                                    {% if form.creditor.errors %}
                                        <div class="text-danger">
                                            {% for error in form.creditor.errors %}
                                                {{ error }}
                                            {% endfor %}
                                        </div>
                                    {% endif %}
                                    <small class="form-text text-muted">Qarz beruvchi boshliqni tanlang</small>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="{{ form.currency.id_for_label }}">
                                        <i class="fas fa-money-bill"></i> Valyuta <span class="text-danger">*</span>
                                    </label>
                                    {{ form.currency }}
                                    {% if form.currency.errors %}
                                        <div class="text-danger">
                                            {% for error in form.currency.errors %}
                                                {{ error }}
                                            {% endfor %}
                                        </div>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="{{ form.debt_amount.id_for_label }}">
                                        <i class="fas fa-dollar-sign"></i> Qarz summasi <span class="text-danger">*</span>
                                    </label>
                                    {{ form.debt_amount }}
                                    {% if form.debt_amount.errors %}
                                        <div class="text-danger">
                                            {% for error in form.debt_amount.errors %}
                                                {{ error }}
                                            {% endfor %}
                                        </div>
                                    {% endif %}
                                    <small class="form-text text-muted">Maksimal: USD - 500$, UZS - 10,000,000 so'm</small>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="{{ form.due_date.id_for_label }}">
                                        <i class="fas fa-calendar"></i> Qaytarish muddati <span class="text-danger">*</span>
                                    </label>
                                    {{ form.due_date }}
                                    {% if form.due_date.errors %}
                                        <div class="text-danger">
                                            {% for error in form.due_date.errors %}
                                                {{ error }}
                                            {% endfor %}
                                        </div>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="{{ form.notes.id_for_label }}">
                                <i class="fas fa-sticky-note"></i> Izoh
                            </label>
                            {{ form.notes }}
                            {% if form.notes.errors %}
                                <div class="text-danger">
                                    {% for error in form.notes.errors %}
                                        {{ error }}
                                    {% endfor %}
                                </div>
                            {% endif %}
                            <small class="form-text text-muted">Qarz maqsadini yoki boshqa ma'lumotlarni kiriting</small>
                        </div>

                        <div class="form-group mt-4">
                            <button type="submit" class="btn btn-success">
                                <i class="fas fa-save"></i> Saqlash
                            </button>
                            <a href="{% url 'sales:debt_list' %}" class="btn btn-secondary">
                                <i class="fas fa-times"></i> Bekor qilish
                            </a>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Yordam kartasi -->
            <div class="card mt-3">
                <div class="card-body">
                    <h5><i class="fas fa-info-circle text-info"></i> Eslatma:</h5>
                    <ul>
                        <li>Siz o'zingiz uchun boshliqdan qarz so'rayapsiz</li>
                        <li>Qarz oluvchi sifatida avtomatik ravishda sizning ismingiz yoziladi</li>
                        <li>Qarzni yaratgandan keyin status avtomatik "Faol" bo'ladi</li>
                        <li>To'lovlarni keyinchalik qo'shishingiz mumkin</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}