{% extends 'base.html' %}
{% load custom_filters %}
{% block page_title %}Yillik Hisobot{% endblock %}

{% block extra_css %}
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: -apple-system, sans-serif; background: #f5f5f5; }
.container { max-width: 1400px; margin: 0 auto; padding: 20px; }

/* Header */
.header { background: #4f46e5; color: white; padding: 20px; border-radius: 8px; margin-bottom: 20px; }
.header h1 { font-size: 24px; margin-bottom: 5px; }
.header-subtitle { opacity: 0.9; font-size: 14px; }

/* Navigation */
.nav { display: flex; gap: 10px; margin-top: 15px; }
.nav a { padding: 8px 16px; background: rgba(255,255,255,0.1); border-radius: 4px; color: white; text-decoration: none; font-size: 14px; }
.nav a.active { background: rgba(255,255,255,0.3); }

/* Filter */
.filter { background: white; padding: 20px; border-radius: 8px; margin-bottom: 20px; }
.filter-row { display: grid; grid-template-columns: 2fr 1fr auto; gap: 15px; align-items: end; }
.form-group label { display: block; font-size: 13px; font-weight: 600; margin-bottom: 5px; color: #374151; }
.form-select { width: 100%; padding: 10px; border: 1px solid #d1d5db; border-radius: 6px; font-size: 14px; }
.btn { padding: 10px 20px; border: none; border-radius: 6px; font-size: 14px; font-weight: 500; cursor: pointer; }
.btn-primary { background: #4f46e5; color: white; }

/* Stats Grid - 2 bo'lim: Telefon va Aksessuar */
.stats-container { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px; }

.stats-section { background: white; padding: 20px; border-radius: 8px; }
.stats-section-title { font-size: 14px; font-weight: 600; color: #6b7280; text-transform: uppercase; margin-bottom: 15px; padding-bottom: 10px; border-bottom: 2px solid; }
.stats-section-title.usd { border-color: #10b981; color: #10b981; }
.stats-section-title.uzs { border-color: #3b82f6; color: #3b82f6; }

.stats-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; }
.stat { text-align: center; padding: 15px; background: #f9fafb; border-radius: 6px; }
.stat-label { font-size: 12px; color: #6b7280; margin-bottom: 8px; }
.stat-value { font-size: 24px; font-weight: 700; color: #111827; }
.stat-value.usd { color: #10b981; }
.stat-value.uzs { color: #3b82f6; }
.stat-currency { font-size: 11px; color: #9ca3af; margin-top: 4px; }

/* Main Content */
.main { display: grid; grid-template-columns: 2fr 1fr; gap: 20px; margin-bottom: 20px; }
.card { background: white; border-radius: 8px; overflow: hidden; }
.card-header { padding: 15px 20px; background: #f8fafc; border-bottom: 1px solid #e5e7eb; font-weight: 600; font-size: 14px; }
.card-body { padding: 20px; }
.chart-container { height: 400px; }

/* Sellers */
.sellers { max-height: 450px; overflow-y: auto; }
.seller { padding: 12px; border-bottom: 1px solid #f3f4f6; display: flex; justify-content: space-between; align-items: center; }
.seller:hover { background: #f9fafb; }
.seller-left { display: flex; align-items: center; gap: 10px; }
.seller-rank { width: 28px; height: 28px; border-radius: 50%; background: #6b7280; color: white; display: flex; align-items: center; justify-content: center; font-size: 13px; font-weight: 600; }
.seller-rank.gold { background: #f59e0b; }
.seller-rank.silver { background: #94a3b8; }
.seller-rank.bronze { background: #d97706; }
.seller-name { font-weight: 600; font-size: 14px; }
.seller-right { text-align: right; }
.seller-amount { font-weight: 700; font-size: 14px; }
.seller-amount.usd { color: #10b981; }
.seller-amount.uzs { color: #3b82f6; }
.seller-details { font-size: 12px; color: #6b7280; margin-top: 4px; }

/* Table */
.table-section { background: white; border-radius: 8px; overflow: hidden; margin-bottom: 20px; }
.table-header { padding: 15px 20px; background: #f8fafc; border-bottom: 1px solid #e5e7eb; font-weight: 600; font-size: 14px; }
.table { width: 100%; border-collapse: collapse; font-size: 13px; }
.table th { background: #f9fafb; padding: 10px 8px; text-align: left; font-weight: 600; font-size: 11px; color: #6b7280; text-transform: uppercase; border-bottom: 1px solid #e5e7eb; }
.table td { padding: 10px 8px; border-bottom: 1px solid #f3f4f6; }
.table tr:hover { background: #f9fafb; }
.amount { font-weight: 600; }
.amount.usd { color: #10b981; }
.amount.uzs { color: #3b82f6; }
.badge { padding: 3px 8px; border-radius: 12px; font-size: 11px; font-weight: 500; }
.badge.phone { background: #dcfce7; color: #166534; }
.badge.accessory { background: #dbeafe; color: #1e40af; }
.badge.exchange { background: #fef3c7; color: #92400e; }

@media (max-width: 1024px) {
  .stats-container { grid-template-columns: 1fr; }
  .main { grid-template-columns: 1fr; }
}

@media (max-width: 768px) {
  .filter-row { grid-template-columns: 1fr; }
  .stats-grid { grid-template-columns: 1fr; }
  .chart-container { height: 300px; }
}
</style>
{% endblock %}

{% block content %}
<div class="container">
  <div class="header">
    <h1>{{ selected_shop.name }}</h1>
    <div class="header-subtitle">{{ year }} Yil</div>
    <div class="nav">
      <a href="{% url 'reports:daily' %}?shop={{ selected_shop.id }}">Kunlik</a>
      <a href="{% url 'reports:monthly' %}?shop={{ selected_shop.id }}">Oylik</a>
      <a href="{% url 'reports:yearly' %}?shop={{ selected_shop.id }}" class="active">Yillik</a>
    </div>
  </div>

  <div class="filter">
    <form method="get">
      <div class="filter-row">
        <div class="form-group">
          <label>Do'kon</label>
          <select name="shop" class="form-select">
            {% for shop in shops %}
            <option value="{{ shop.id }}" {% if shop == selected_shop %}selected{% endif %}>{{ shop.name }}</option>
            {% endfor %}
          </select>
        </div>
        <div class="form-group">
          <label>Yil</label>
          <select name="year" class="form-select">
            {% for y in years %}
            <option value="{{ y }}" {% if y == year %}selected{% endif %}>{{ y }}</option>
            {% endfor %}
          </select>
        </div>
        <button type="submit" class="btn btn-primary">Ko'rsat</button>
      </div>
    </form>
  </div>

  <!-- TELEFON VA AKSESSUAR STATISTIKA (ALOHIDA) -->
  <div class="stats-container">
    <!-- TELEFON (Dollar) -->
    <div class="stats-section">
      <div class="stats-section-title usd">📱 TELEFON (Dollar)</div>
      <div class="stats-grid">
        <div class="stat">
          <div class="stat-label">Savdo</div>
          <div class="stat-value usd">${{ yearly_stats.totals.phone_sales|floatformat:0|default:"0" }}</div>
          <div class="stat-currency">USD</div>
        </div>
        <div class="stat">
          <div class="stat-label">Naqd</div>
          <div class="stat-value usd">${{ yearly_stats.totals.phone_cash|floatformat:0|default:"0" }}</div>
          <div class="stat-currency">USD</div>
        </div>
        <div class="stat">
          <div class="stat-label">Soni</div>
          <div class="stat-value usd">{{ yearly_stats.counts.phone|default:0 }}</div>
          <div class="stat-currency">dona</div>
        </div>
        <div class="stat">
          <div class="stat-label">Foyda</div>
          <div class="stat-value usd">${{ yearly_stats.profits.phone_profit|floatformat:0|default:"0" }}</div>
          <div class="stat-currency">USD</div>
        </div>
      </div>
    </div>

    <!-- AKSESSUAR (So'm) -->
    <div class="stats-section">
      <div class="stats-section-title uzs">🎧 AKSESSUAR (So'm)</div>
      <div class="stats-grid">
        <div class="stat">
          <div class="stat-label">Savdo</div>
          <div class="stat-value uzs">{{ yearly_stats.totals.accessory_sales|floatformat:0|default:"0" }}</div>
          <div class="stat-currency">UZS</div>
        </div>
        <div class="stat">
          <div class="stat-label">Naqd</div>
          <div class="stat-value uzs">{{ yearly_stats.totals.accessory_cash|floatformat:0|default:"0" }}</div>
          <div class="stat-currency">UZS</div>
        </div>
        <div class="stat">
          <div class="stat-label">Soni</div>
          <div class="stat-value uzs">{{ yearly_stats.counts.accessory|default:0 }}</div>
          <div class="stat-currency">dona</div>
        </div>
        <div class="stat">
          <div class="stat-label">Foyda</div>
          <div class="stat-value uzs">{{ yearly_stats.profits.accessory_profit|floatformat:0|default:"0" }}</div>
          <div class="stat-currency">UZS</div>
        </div>
      </div>
    </div>
  </div>

  <div class="main">
    <div class="card">
      <div class="card-header">Oylik Dinamika</div>
      <div class="card-body">
        <div class="chart-container">
          <canvas id="chart"></canvas>
        </div>
      </div>
    </div>

    <div class="card">
      <div class="card-header">TOP Sotuvchilar ({{ yearly_sellers_stats|length }})</div>
      <div class="card-body">
        <div class="sellers">
          {% for seller_stat in yearly_sellers_stats %}
          <div class="seller">
            <div class="seller-left">
              <div class="seller-rank {% if forloop.counter == 1 %}gold{% elif forloop.counter == 2 %}silver{% elif forloop.counter == 3 %}bronze{% endif %}">{{ forloop.counter }}</div>
              <div class="seller-name">{{ seller_stat.seller.get_full_name|default:seller_stat.seller.username }}</div>
            </div>
            <div class="seller-right">
              <div class="seller-amount usd">${{ seller_stat.phone_sales|floatformat:0 }}</div>
              <div class="seller-amount uzs">{{ seller_stat.accessory_sales|floatformat:0 }} so'm</div>
              <div class="seller-details">T:{{ seller_stat.phone_count }} • A:{{ seller_stat.accessory_count }} • {{ seller_stat.working_days }}kun</div>
            </div>
          </div>
          {% empty %}
          <div style="text-align: center; padding: 40px; color: #9ca3af;">Ma'lumot yo'q</div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>

  <!-- OYLIK TAFSILOT -->
  <div class="table-section">
    <div class="table-header">Oylik Tafsilot</div>
    <table class="table">
      <thead>
        <tr>
          <th>Oy</th>
          <th>Tel.Savdo ($)</th>
          <th>Tel.Naqd ($)</th>
          <th>Tel.Foyda ($)</th>
          <th>Aks.Savdo (so'm)</th>
          <th>Aks.Naqd (so'm)</th>
          <th>Aks.Foyda (so'm)</th>
          <th>T/A/E</th>
          <th>Kunlar</th>
        </tr>
      </thead>
      <tbody>
        {% for monthly in yearly_stats.monthly_stats %}
        <tr>
          <td>{{ month_names|get_item:monthly.month|default:monthly.month }}-oy</td>
          <td class="amount usd">${{ monthly.totals.phone_sales_usd|floatformat:0|default:"0" }}</td>
          <td class="amount usd">${{ monthly.totals.phone_cash_usd|floatformat:0|default:"0" }}</td>
          <td class="amount usd">${{ monthly.profits.phone_profit|floatformat:0|default:"0" }}</td>
          <td class="amount uzs">{{ monthly.totals.accessory_sales_uzs|floatformat:0|default:"0" }}</td>
          <td class="amount uzs">{{ monthly.totals.accessory_cash_uzs|floatformat:0|default:"0" }}</td>
          <td class="amount uzs">{{ monthly.profits.accessory_profit|floatformat:0|default:"0" }}</td>
          <td>
            <span class="badge phone">{{ monthly.counts.phone }}</span>
            <span class="badge accessory">{{ monthly.counts.accessory }}</span>
            <span class="badge exchange">{{ monthly.counts.exchange }}</span>
          </td>
          <td>{{ monthly.period.working_days }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  <!-- SOTUVCHILAR TAFSILOT -->
  {% if yearly_sellers_stats %}
  <div class="table-section">
    <div class="table-header">Sotuvchilar Tafsiloti</div>
    <table class="table">
      <thead>
        <tr>
          <th>#</th>
          <th>Sotuvchi</th>
          <th>Tel.Savdo ($)</th>
          <th>Tel.Foyda ($)</th>
          <th>Aks.Savdo (so'm)</th>
          <th>Aks.Foyda (so'm)</th>
          <th>T/A/E</th>
          <th>Kunlar</th>
        </tr>
      </thead>
      <tbody>
        {% for seller_stat in yearly_sellers_stats %}
        <tr>
          <td>{{ forloop.counter }}</td>
          <td>{{ seller_stat.seller.get_full_name|default:seller_stat.seller.username }}</td>
          <td class="amount usd">${{ seller_stat.phone_sales|floatformat:0 }}</td>
          <td class="amount usd">${{ seller_stat.phone_profit|floatformat:0 }}</td>
          <td class="amount uzs">{{ seller_stat.accessory_sales|floatformat:0 }}</td>
          <td class="amount uzs">{{ seller_stat.accessory_profit|floatformat:0 }}</td>
          <td>
            <span class="badge phone">{{ seller_stat.phone_count }}</span>
            <span class="badge accessory">{{ seller_stat.accessory_count }}</span>
            <span class="badge exchange">{{ seller_stat.exchange_count }}</span>
          </td>
          <td>{{ seller_stat.working_days }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
  {% endif %}
</div>

<script>
const monthlyData = [];
{% for monthly in yearly_stats.monthly_stats %}
monthlyData.push({
  month: '{{ month_names|get_item:monthly.month|default:monthly.month|escapejs }}-oy',
  phoneSales: parseFloat('{{ monthly.totals.phone_sales_usd|default:0 }}') || 0,
  phoneCash: parseFloat('{{ monthly.totals.phone_cash_usd|default:0 }}') || 0,
  phoneProfit: parseFloat('{{ monthly.profits.phone_profit|default:0 }}') || 0,
  accessorySales: parseFloat('{{ monthly.totals.accessory_sales_uzs|default:0 }}') || 0,
  accessoryCash: parseFloat('{{ monthly.totals.accessory_cash_uzs|default:0 }}') || 0,
  accessoryProfit: parseFloat('{{ monthly.profits.accessory_profit|default:0 }}') || 0,
  phoneCount: parseInt('{{ monthly.counts.phone|default:0 }}') || 0,
  accessoryCount: parseInt('{{ monthly.counts.accessory|default:0 }}') || 0
});
{% endfor %}

const data = {
  labels: monthlyData.map(d => d.month),
  datasets: [
    {
      label: 'Telefon Savdo ($)',
      data: monthlyData.map(d => d.phoneSales),
      borderColor: '#10b981',
      backgroundColor: 'rgba(16,185,129,0.1)',
      tension: 0.4,
      borderWidth: 2,
      yAxisID: 'y'
    },
    {
      label: 'Telefon Naqd ($)',
      data: monthlyData.map(d => d.phoneCash),
      borderColor: '#059669',
      borderDash: [5,5],
      tension: 0.4,
      borderWidth: 2,
      yAxisID: 'y'
    },
    {
      label: 'Aksessuar Savdo (so\'m)',
      data: monthlyData.map(d => d.accessorySales),
      borderColor: '#3b82f6',
      backgroundColor: 'rgba(59,130,246,0.1)',
      tension: 0.4,
      borderWidth: 2,
      yAxisID: 'y1'
    },
    {
      label: 'Aksessuar Naqd (so\'m)',
      data: monthlyData.map(d => d.accessoryCash),
      borderColor: '#2563eb',
      borderDash: [5,5],
      tension: 0.4,
      borderWidth: 2,
      yAxisID: 'y1'
    }
  ]
};

new Chart(document.getElementById('chart'), {
  type: 'line',
  data: data,
  options: {
    responsive: true,
    maintainAspectRatio: false,
    interaction: { mode: 'index', intersect: false },
    plugins: {
      legend: { position: 'top' },
      tooltip: {
        callbacks: {
          label: ctx => {
            const label = ctx.dataset.label;
            const value = ctx.parsed.y;
            return label.includes('so\'m') ?
              `${label}: ${value.toLocaleString('uz-UZ')} so'm` :
              `${label}: ${value.toFixed(2)}`;
          }
        }
      }
    },
    scales: {
      y: {
        type: 'linear',
        position: 'left',
        beginAtZero: true,
        ticks: { color: '#10b981' },
        title: { display: true, text: 'Dollar ($)', color: '#10b981' }
      },
      y1: {
        type: 'linear',
        position: 'right',
        beginAtZero: true,
        grid: { drawOnChartArea: false },
        ticks: {
          color: '#3b82f6',
          callback: v => (v/1000).toFixed(0) + 'K'
        },
        title: { display: true, text: 'So\'m (ming)', color: '#3b82f6' }
      }
    }
  }
});
</script>
{% endblock %}