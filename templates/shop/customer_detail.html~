{% extends 'base.html' %}

{% block title %}Mijoz Tafsilotlari - {{ customer.name }}{% endblock %}
{% block page_title %}Mijoz: {{ customer.name }}{% endblock %}

{% block extra_css %}
<style>
.customer-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 20px;
    color: white;
    padding: 2rem;
    margin-bottom: 2rem;
}

.customer-avatar-lg {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background: rgba(255,255,255,0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    font-weight: 600;
    margin-right: 1.5rem;
}

.info-card {
    background: white;
    border-radius: 15px;
    padding: 1.5rem;
    box-shadow: 0 5px 20px rgba(0,0,0,0.08);
    margin-bottom: 1.5rem;
    border: none;
}

.info-card .card-header {
    background: none;
    border-bottom: 2px solid #f1f5f9;
    padding: 0 0 1rem 0;
    margin-bottom: 1rem;
}

.info-card .card-title {
    color: #2d3748;
    font-weight: 600;
    font-size: 1.1rem;
    margin: 0;
}

.badge-custom {
    font-size: 0.8rem;
    padding: 0.4rem 0.8rem;
    border-radius: 10px;
}

.table-custom {
    font-size: 0.9rem;
    width: 100%;
}

.table-custom th {
    background: #f8fafc;
    color: #4a5568;
    font-weight: 600;
    border: none;
    padding: 0.75rem;
    white-space: nowrap;
}

.table-custom td {
    padding: 0.75rem;
    border-color: #f1f5f9;
    vertical-align: middle;
}

.amount-usd {
    color: #48bb78;
    font-weight: 600;
}

.amount-uzs {
    color: #4299e1;
    font-weight: 600;
}

.payment-badge {
    font-size: 0.75rem;
    padding: 0.3rem 0.6rem;
    border-radius: 6px;
    font-weight: 500;
}

.payment-badge.cash {
    background: rgba(34, 197, 94, 0.1);
    color: #16a34a;
    border: 1px solid rgba(34, 197, 94, 0.2);
}

.payment-badge.card {
    background: rgba(59, 130, 246, 0.1);
    color: #2563eb;
    border: 1px solid rgba(59, 130, 246, 0.2);
}

.payment-badge.credit {
    background: rgba(245, 158, 11, 0.1);
    color: #d97706;
    border: 1px solid rgba(245, 158, 11, 0.2);
}

.payment-badge.debt {
    background: rgba(239, 68, 68, 0.1);
    color: #dc2626;
    border: 1px solid rgba(239, 68, 68, 0.2);
}

.debt-badge {
    font-size: 0.75rem;
    padding: 0.3rem 0.6rem;
    border-radius: 8px;
}

.debt-badge.usd {
    background: rgba(239, 68, 68, 0.1);
    color: #e53e3e;
    border: 1px solid rgba(239, 68, 68, 0.2);
}

.debt-badge.uzs {
    background: rgba(245, 158, 11, 0.1);
    color: #d69e2e;
    border: 1px solid rgba(245, 158, 11, 0.2);
}

.stat-badge {
    background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 15px;
    font-size: 0.8rem;
    font-weight: 600;
}

.empty-state {
    padding: 2rem;
    text-align: center;
    color: #a0aec0;
}

.empty-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    opacity: 0.5;
}

.action-btns {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
}

.btn-custom {
    border-radius: 10px;
    padding: 0.5rem 1rem;
    font-weight: 500;
    transition: all 0.3s ease;
}

.btn-custom:hover {
    transform: translateY(-2px);
}

.imei-badge {
    background: #f1f5f9;
    color: #64748b;
    font-family: monospace;
    font-size: 0.8rem;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    border: 1px solid #e2e8f0;
}

.payment-summary {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 0.5rem;
    margin-top: 0.5rem;
}

.payment-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 0.5rem;
    border-radius: 8px;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
}

.payment-label {
    font-size: 0.7rem;
    color: #64748b;
    margin-bottom: 0.25rem;
}

.payment-value {
    font-size: 0.8rem;
    font-weight: 600;
}

@media (max-width: 768px) {
    .customer-header {
        padding: 1.5rem;
        text-align: center;
    }

    .customer-avatar-lg {
        margin: 0 auto 1rem;
    }

    .table-custom {
        font-size: 0.8rem;
    }

    .action-btns {
        justify-content: center;
    }

    .d-mobile-none {
        display: none;
    }

    .payment-summary {
        grid-template-columns: repeat(2, 1fr);
    }
}
</style>
{% endblock %}

{% block content %}
<div class="container py-4">
    <!-- Customer Header -->
    <div class="customer-header">
        <div class="row align-items-center">
            <div class="col-md-8">
                <div class="d-flex align-items-center">
                    {% if customer.image %}
                    <img src="{{ customer.image.url }}" alt="{{ customer.name }}" class="customer-avatar-lg" style="object-fit: cover;">
                    {% else %}
                    <div class="customer-avatar-lg">
                        {{ customer.name|first|upper }}
                    </div>
                    {% endif %}
                    <div>
                        <h1 class="h3 mb-1">{{ customer.name }}</h1>
                        <p class="mb-0 opacity-90">{{ customer.phone_number }}</p>
                        {% if customer.notes %}
                        <p class="mb-0 opacity-80 mt-1"><small>{{ customer.notes }}</small></p>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="col-md-4 text-md-end text-center mt-3 mt-md-0">
                <div class="action-btns">
                    <a href="{% url 'shop:customer_list' %}" class="btn btn-light btn-custom">
                        <i class="fas fa-arrow-left me-1"></i> Orqaga
                    </a>
                    {% if is_boss or request.user.userprofile.role == 'seller' %}
                    <a href="{% url 'shop:customer_edit' customer.pk %}" class="btn btn-warning btn-custom">
                        <i class="fas fa-edit me-1"></i> Tahrirlash
                    </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- Statistics Row -->
    <div class="row">
        <div class="col-md-3 col-6 mb-3">
            <div class="info-card text-center">
                <div class="stat-number text-primary">{{ total_purchases_count }}</div>
                <div class="stat-label">Jami Xaridlar</div>
            </div>
        </div>
        <div class="col-md-3 col-6 mb-3">
            <div class="info-card text-center">
                <div class="stat-number amount-usd">${{ customer.total_purchases_usd|floatformat:0 }}</div>
                <div class="stat-label">Xaridlar ($)</div>
            </div>
        </div>
        <div class="col-md-3 col-6 mb-3">
            <div class="info-card text-center">
                <div class="stat-number amount-uzs">{{ customer.total_purchases_uzs|floatformat:0 }}</div>
                <div class="stat-label">Xaridlar (so'm)</div>
            </div>
        </div>
        <div class="col-md-3 col-6 mb-3">
            <div class="info-card text-center">
                <div class="stat-number text-danger">{{ customer.active_debts_count }}</div>
                <div class="stat-label">Faol Qarzlar</div>
            </div>
        </div>
    </div>

    <div class="row">
        <!-- Left Column -->
        <div class="col-lg-8">
            <!-- Telefon Sotuvlari -->
            <div class="info-card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="card-title">
                        <i class="fas fa-mobile-alt me-2"></i>Telefon Sotuvlari
                    </h5>
                    <span class="stat-badge">{{ phone_sales_count }}</span>
                </div>
                <div class="table-responsive">
                    <table class="table table-custom">
                        <thead>
                            <tr>
                                <th>Telefon</th>
                                <th>IMEI</th>
                                <th>Sotish Narxi</th>
                                <th>To'lovlar</th>
                                <th class="d-mobile-none">Sana</th>
                                <th class="d-mobile-none">Sotuvchi</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for sale in customer.phone_sales.all %}
                            <tr>
                                <td>
                                    <div>
                                        <strong>{{ sale.phone.phone_model }} {{ sale.phone.memory_size }}</strong>
                                        <div class="text-muted small">{{ sale.phone.color|default:"" }}</div>
                                    </div>
                                </td>
                                <td>
                                    <span class="imei-badge">{{ sale.phone.imei }}</span>
                                </td>
                                <td>
                                    <span class="amount-usd">${{ sale.sale_price|floatformat:0 }}</span>
                                </td>
                                <td>
                                    <div class="payment-summary">
                                        {% if sale.cash_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Naqd</span>
                                            <span class="payment-value payment-badge cash">${{ sale.cash_amount|floatformat:0 }}</span>
                                        </div>
                                        {% endif %}
                                        {% if sale.card_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Karta</span>
                                            <span class="payment-value payment-badge card">${{ sale.card_amount|floatformat:0 }}</span>
                                        </div>
                                        {% endif %}
                                        {% if sale.credit_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Nasiya</span>
                                            <span class="payment-value payment-badge credit">${{ sale.credit_amount|floatformat:0 }}</span>
                                        </div>
                                        {% endif %}
                                        {% if sale.debt_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Qarz</span>
                                            <span class="payment-value payment-badge debt">${{ sale.debt_amount|floatformat:0 }}</span>
                                        </div>
                                        {% endif %}
                                    </div>
                                </td>
                                <td class="d-mobile-none">
                                    {{ sale.sale_date|date:"d.m.Y" }}
                                </td>
                                <td class="d-mobile-none">
                                    {% if sale.salesman %}
                                        {{ sale.salesman.get_full_name|default:sale.salesman.username }}
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                            </tr>
                            {% empty %}
                            <tr>
                                <td colspan="6" class="empty-state">
                                    <div class="empty-icon">
                                        <i class="fas fa-mobile-alt"></i>
                                    </div>
                                    <div>Telefon sotuvlari topilmadi</div>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Aksessuar Sotuvlari -->
            <div class="info-card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="card-title">
                        <i class="fas fa-headphones me-2"></i>Aksessuar Sotuvlari
                    </h5>
                    <span class="stat-badge">{{ accessory_sales_count }}</span>
                </div>
                <div class="table-responsive">
                    <table class="table table-custom">
                        <thead>
                            <tr>
                                <th>Aksessuar</th>
                                <th>Soni</th>
                                <th>Umumiy Narx</th>
                                <th>To'lovlar</th>
                                <th class="d-mobile-none">Sana</th>
                                <th class="d-mobile-none">Sotuvchi</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for sale in customer.accessory_sales.all %}
                            <tr>
                                <td>
                                    <strong>{{ sale.accessory.name }}</strong>
                                    {% if sale.accessory.code %}
                                    <div class="text-muted small">Kod: {{ sale.accessory.code }}</div>
                                    {% endif %}
                                </td>
                                <td>
                                    <span class="badge bg-secondary">{{ sale.quantity }} dona</span>
                                </td>
                                <td>
                                    <span class="amount-uzs">{{ sale.total_price|floatformat:0 }} so'm</span>
                                </td>
                                <td>
                                    <div class="payment-summary">
                                        {% if sale.cash_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Naqd</span>
                                            <span class="payment-value payment-badge cash">{{ sale.cash_amount|floatformat:0 }} so'm</span>
                                        </div>
                                        {% endif %}
                                        {% if sale.card_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Karta</span>
                                            <span class="payment-value payment-badge card">{{ sale.card_amount|floatformat:0 }} so'm</span>
                                        </div>
                                        {% endif %}
                                        {% if sale.credit_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Nasiya</span>
                                            <span class="payment-value payment-badge credit">{{ sale.credit_amount|floatformat:0 }} so'm</span>
                                        </div>
                                        {% endif %}
                                        {% if sale.debt_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Qarz</span>
                                            <span class="payment-value payment-badge debt">{{ sale.debt_amount|floatformat:0 }} so'm</span>
                                        </div>
                                        {% endif %}
                                    </div>
                                </td>
                                <td class="d-mobile-none">
                                    {{ sale.sale_date|date:"d.m.Y" }}
                                </td>
                                <td class="d-mobile-none">
                                    {% if sale.salesman %}
                                        {{ sale.salesman.get_full_name|default:sale.salesman.username }}
                                    {% else %}
                                        -
                                    {% endif %}
                                </td>
                            </tr>
                            {% empty %}
                            <tr>
                                <td colspan="6" class="empty-state">
                                    <div class="empty-icon">
                                        <i class="fas fa-headphones"></i>
                                    </div>
                                    <div>Aksessuar sotuvlari topilmadi</div>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Telefon Almashtirish -->
            <div class="info-card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="card-title">
                        <i class="fas fa-exchange-alt me-2"></i>Telefon Almashtirish
                    </h5>
                    <span class="stat-badge">{{ exchange_count }}</span>
                </div>
                <div class="table-responsive">
                    <table class="table table-custom">
                        <thead>
                            <tr>
                                <th>Eski Telefon</th>
                                <th>Yangi Telefon</th>
                                <th>Yangi IMEI</th>
                                <th>Narx Farqi</th>
                                <th>To'lovlar</th>
                                <th class="d-mobile-none">Sana</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for exchange in customer.phone_exchanges.all %}
                            <tr>
                                <td>
                                    <div>
                                        <strong>{{ exchange.old_phone_model }}</strong>
                                        <div class="text-muted small">{{ exchange.old_phone_memory }}</div>
                                        {% if exchange.old_phone_imei %}
                                        <div class="imei-badge mt-1">{{ exchange.old_phone_imei }}</div>
                                        {% endif %}
                                    </div>
                                </td>
                                <td>
                                    <div>
                                        <strong>{{ exchange.new_phone.phone_model }}</strong>
                                        <div class="text-muted small">{{ exchange.new_phone.memory_size }}</div>
                                    </div>
                                </td>
                                <td>
                                    <span class="imei-badge">{{ exchange.new_phone.imei }}</span>
                                </td>
                                <td>
                                    <span class="amount-usd">${{ exchange.price_difference|floatformat:0 }}</span>
                                </td>
                                <td>
                                    <div class="payment-summary">
                                        {% if exchange.cash_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Naqd</span>
                                            <span class="payment-value payment-badge cash">${{ exchange.cash_amount|floatformat:0 }}</span>
                                        </div>
                                        {% endif %}
                                        {% if exchange.card_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Karta</span>
                                            <span class="payment-value payment-badge card">${{ exchange.card_amount|floatformat:0 }}</span>
                                        </div>
                                        {% endif %}
                                        {% if exchange.credit_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Nasiya</span>
                                            <span class="payment-value payment-badge credit">${{ exchange.credit_amount|floatformat:0 }}</span>
                                        </div>
                                        {% endif %}
                                        {% if exchange.debt_amount > 0 %}
                                        <div class="payment-item">
                                            <span class="payment-label">Qarz</span>
                                            <span class="payment-value payment-badge debt">${{ exchange.debt_amount|floatformat:0 }}</span>
                                        </div>
                                        {% endif %}
                                    </div>
                                </td>
                                <td class="d-mobile-none">
                                    {{ exchange.exchange_date|date:"d.m.Y" }}
                                </td>
                            </tr>
                            {% empty %}
                            <tr>
                                <td colspan="6" class="empty-state">
                                    <div class="empty-icon">
                                        <i class="fas fa-exchange-alt"></i>
                                    </div>
                                    <div>Telefon almashtirishlar topilmadi</div>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Right Column -->
        <div class="col-lg-4">
            <!-- Debt Summary -->
            <div class="info-card">
                <div class="card-header">
                    <h5 class="card-title">
                        <i class="fas fa-credit-card me-2"></i>Qarz Ma'lumotlari
                    </h5>
                </div>
                <div class="d-grid gap-2">
                    {% if has_phone_debt %}
                    <div class="d-flex justify-content-between align-items-center p-2 border rounded">
                        <span>Telefon qarzi:</span>
                        <span class="debt-badge usd">${{ phone_debt_usd|floatformat:0 }}</span>
                    </div>
                    {% endif %}
                    {% if has_accessory_debt %}
                    <div class="d-flex justify-content-between align-items-center p-2 border rounded">
                        <span>Aksessuar qarzi:</span>
                        <span class="debt-badge uzs">{{ accessory_debt_uzs|floatformat:0 }} so'm</span>
                    </div>
                    {% endif %}
                    {% if has_exchange_debt %}
                    <div class="d-flex justify-content-between align-items-center p-2 border rounded">
                        <span>Almashtirish qarzi:</span>
                        <span class="debt-badge usd">${{ exchange_debt_usd|floatformat:0 }}</span>
                    </div>
                    {% endif %}
                    {% if not has_phone_debt and not has_accessory_debt and not has_exchange_debt %}
                    <div class="text-center p-3">
                        <i class="fas fa-check-circle text-success fa-2x mb-2"></i>
                        <div>Qarzlar mavjud emas</div>
                    </div>
                    {% endif %}
                </div>
            </div>

            <!-- Purchase Statistics -->
            <div class="info-card">
                <div class="card-header">
                    <h5 class="card-title">
                        <i class="fas fa-chart-pie me-2"></i>Xarid Statistikasi
                    </h5>
                </div>
                <div class="d-grid gap-2">
                    <div class="d-flex justify-content-between align-items-center p-2 border rounded">
                        <span>Telefon sotuvlari:</span>
                        <span class="stat-badge">{{ phone_sales_count }}</span>
                    </div>
                    <div class="d-flex justify-content-between align-items-center p-2 border rounded">
                        <span>Aksessuar sotuvlari:</span>
                        <span class="stat-badge">{{ accessory_sales_count }}</span>
                    </div>
                    <div class="d-flex justify-content-between align-items-center p-2 border rounded">
                        <span>Almashtirishlar:</span>
                        <span class="stat-badge">{{ exchange_count }}</span>
                    </div>
                    <div class="d-flex justify-content-between align-items-center p-2 border rounded">
                        <span>Jami xaridlar:</span>
                        <span class="stat-badge">{{ total_purchases_count }}</span>
                    </div>
                </div>
            </div>

            <!-- Customer Information -->
            <div class="info-card">
                <div class="card-header">
                    <h5 class="card-title">
                        <i class="fas fa-info-circle me-2"></i>Ma'lumotlar
                    </h5>
                </div>
                <div class="d-grid gap-2">
                    <div class="p-2 border rounded">
                        <small class="text-muted">Qo'shilgan sana</small>
                        <div>{{ customer.created_at|date:"d.m.Y" }}</div>
                    </div>
                    <div class="p-2 border rounded">
                        <small class="text-muted">Qo'shgan foydalanuvchi</small>
                        <div>
                            {% if customer.created_by %}
                                {{ customer.created_by.get_full_name|default:customer.created_by.username }}
                            {% else %}
                                -
                            {% endif %}
                        </div>
                    </div>
                    <div class="p-2 border rounded">
                        <small class="text-muted">Telefon raqami</small>
                        <div class="fw-bold">{{ customer.phone_number }}</div>
                    </div>
                    {% if customer.notes %}
                    <div class="p-2 border rounded">
                        <small class="text-muted">Izohlar</small>
                        <div>{{ customer.notes }}</div>
                    </div>
                    {% endif %}
                </div>
            </div>

            <!-- Active Debts -->
            {% if active_debts %}
            <div class="info-card">
                <div class="card-header">
                    <h5 class="card-title">
                        <i class="fas fa-exclamation-triangle me-2"></i>Faol Qarzlar
                    </h5>
                </div>
                <div class="d-grid gap-2">
                    {% for debt in active_debts %}
                    <div class="p-2 border rounded">
                        <div class="d-flex justify-content-between align-items-center mb-1">
                            <span class="fw-bold">{{ debt.get_debt_type_display }}</span>
                            {% if debt.currency == 'USD' %}
                            <span class="amount-usd">${{ debt.debt_amount|floatformat:0 }}</span>
                            {% else %}
                            <span class="amount-uzs">{{ debt.debt_amount|floatformat:0 }} so'm</span>
                            {% endif %}
                        </div>
                        <div class="d-flex justify-content-between small text-muted">
                            <span>To'langan:</span>
                            <span>
                                {% if debt.currency == 'USD' %}
                                ${{ debt.paid_amount|floatformat:0 }}
                                {% else %}
                                {{ debt.paid_amount|floatformat:0 }} so'm
                                {% endif %}
                            </span>
                        </div>
                        <div class="d-flex justify-content-between small text-muted">
                            <span>Qoldiq:</span>
                            <span class="text-danger">
                                {% if debt.currency == 'USD' %}
                                ${{ debt.remaining_amount|floatformat:0 }}
                                {% else %}
                                {{ debt.remaining_amount|floatformat:0 }} so'm
                                {% endif %}
                            </span>
                        </div>
                        {% if debt.due_date %}
                        <div class="d-flex justify-content-between small text-muted">
                            <span>Muddati:</span>
                            <span>{{ debt.due_date|date:"d.m.Y" }}</span>
                        </div>
                        {% endif %}
                    </div>
                    {% endfor %}
                </div>
            </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}