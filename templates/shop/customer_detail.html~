{% extends 'base.html' %}
{% load static %}

{% block title %}{{ customer.name }} - Mijoz Tafsilotlari{% endblock %}

{% block extra_css %}
<style>
.header-section {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 20px;
    color: white;
    padding: 2rem;
    margin-bottom: 2rem;
    position: relative;
    overflow: hidden;
}

.customer-avatar-lg {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 2rem;
    margin-right: 1.5rem;
}

.customer-info h1 {
    font-weight: 700;
    margin-bottom: 0.5rem;
}

.customer-meta {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
}

.meta-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    opacity: 0.9;
}

.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin-bottom: 2rem;
}

.stat-card {
    background: white;
    border-radius: 15px;
    padding: 1.5rem;
    text-align: center;
    box-shadow: 0 5px 20px rgba(0,0,0,0.08);
    border: none;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}

.stat-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
}

.stat-card.primary::before { background: linear-gradient(135deg, #4299e1 0%, #3182ce 100%); }
.stat-card.success::before { background: linear-gradient(135deg, #48bb78 0%, #38a169 100%); }
.stat-card.warning::before { background: linear-gradient(135deg, #ed8936 0%, #dd6b20 100%); }
.stat-card.danger::before { background: linear-gradient(135deg, #f56565 0%, #e53e3e 100%); }

.stat-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0,0,0,0.12);
}

.stat-icon {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1rem;
    font-size: 1.3rem;
    color: white;
}

.stat-card.primary .stat-icon { background: linear-gradient(135deg, #4299e1 0%, #3182ce 100%); }
.stat-card.success .stat-icon { background: linear-gradient(135deg, #48bb78 0%, #38a169 100%); }
.stat-card.warning .stat-icon { background: linear-gradient(135deg, #ed8936 0%, #dd6b20 100%); }
.stat-card.danger .stat-icon { background: linear-gradient(135deg, #f56565 0%, #e53e3e 100%); }

.stat-number {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
}

.stat-card.primary .stat-number { color: #4299e1; }
.stat-card.success .stat-number { color: #48bb78; }
.stat-card.warning .stat-number { color: #ed8936; }
.stat-card.danger .stat-number { color: #f56565; }

.stat-label {
    color: #718096;
    font-weight: 500;
    font-size: 0.9rem;
}

.section-card {
    background: white;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    border: none;
    overflow: hidden;
    margin-bottom: 2rem;
}

.section-header {
    background: linear-gradient(135deg, #f8fafc 0%, #edf2f7 100%);
    padding: 1.2rem 1.5rem;
    border-bottom: 1px solid #e2e8f0;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.section-title {
    color: #2d3748;
    font-weight: 700;
    font-size: 1.1rem;
    margin: 0;
}

.section-count {
    background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
    color: white;
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 600;
}

.detailed-table {
    margin: 0;
    font-size: 0.9rem;
    width: 100%;
}

.detailed-table thead th {
    background: #f8fafc;
    color: #4a5568;
    font-weight: 600;
    border: none;
    padding: 1rem;
    border-bottom: 2px solid #e2e8f0;
    text-align: left;
}

.detailed-table tbody td {
    padding: 1rem;
    border: none;
    border-bottom: 1px solid #f1f5f9;
    vertical-align: middle;
}

.detailed-table tbody tr {
    transition: all 0.3s ease;
}

.detailed-table tbody tr:hover {
    background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
}

.phone-model {
    font-weight: 600;
    color: #2d3748;
    margin: 0;
}

.phone-details {
    color: #718096;
    font-size: 0.8rem;
    margin: 0.2rem 0 0 0;
}

.amount-cell {
    text-align: right;
}

.amount-main {
    font-weight: 700;
    font-size: 1rem;
    margin: 0;
}

.amount-sub {
    color: #718096;
    font-size: 0.8rem;
    margin: 0.2rem 0 0 0;
}

.profit-badge {
    padding: 0.3rem 0.6rem;
    border-radius: 12px;
    font-size: 0.75rem;
    font-weight: 600;
}

.profit-positive {
    background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
    color: white;
}

.profit-negative {
    background: linear-gradient(135deg, #f56565 0%, #e53e3e 100%);
    color: white;
}

.payment-methods {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
}

.payment-badge {
    display: inline-block;
    padding: 0.2rem 0.5rem;
    border-radius: 8px;
    font-size: 0.7rem;
    font-weight: 500;
    text-align: center;
}

.payment-cash { background: linear-gradient(135deg, #48bb78 0%, #38a169 100%); color: white; }
.payment-card { background: linear-gradient(135deg, #4299e1 0%, #3182ce 100%); color: white; }
.payment-credit { background: linear-gradient(135deg, #ed8936 0%, #dd6b20 100%); color: white; }
.payment-debt { background: linear-gradient(135deg, #f56565 0%, #e53e3e 100%); color: white; }

.source-badge {
    padding: 0.3rem 0.6rem;
    border-radius: 8px;
    font-size: 0.75rem;
    font-weight: 500;
    background: #e2e8f0;
    color: #4a5568;
}

.empty-state {
    padding: 3rem;
    text-align: center;
    color: #a0aec0;
}

.empty-icon {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    opacity: 0.5;
}

.empty-title {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.4rem;
}

.empty-subtitle {
    font-size: 0.85rem;
}

.action-buttons {
    display: flex;
    gap: 0.5rem;
    justify-content: flex-end;
}

.btn-back {
    background: linear-gradient(135deg, #a0aec0 0%, #718096 100%);
    color: white;
    border: none;
    border-radius: 12px;
    padding: 0.6rem 1.2rem;
    font-weight: 600;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    font-size: 0.9rem;
}

.btn-back:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(160, 174, 192, 0.3);
    color: white;
}

.btn-edit {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 12px;
    padding: 0.6rem 1.2rem;
    font-weight: 600;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    font-size: 0.9rem;
}

.btn-edit:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
    color: white;
}

.btn-disabled {
    background: #e2e8f0;
    color: #a0aec0;
    border: none;
    border-radius: 12px;
    padding: 0.6rem 1.2rem;
    font-weight: 600;
    cursor: not-allowed;
    opacity: 0.5;
}

/* Responsive Design */
@media (max-width: 768px) {
    .header-section {
        padding: 1.5rem;
    }

    .customer-avatar-lg {
        width: 60px;
        height: 60px;
        font-size: 1.5rem;
        margin-right: 1rem;
    }

    .customer-info h1 {
        font-size: 1.5rem;
    }

    .customer-meta {
        gap: 1rem;
    }

    .meta-item {
        font-size: 0.8rem;
    }

    .stats-grid {
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 0.8rem;
    }

    .stat-card {
        padding: 1rem;
    }

    .stat-number {
        font-size: 1.5rem;
    }

    .stat-icon {
        width: 40px;
        height: 40px;
        font-size: 1.1rem;
    }

    .section-header {
        padding: 1rem;
    }

    .detailed-table {
        font-size: 0.8rem;
    }

    .detailed-table thead th,
    .detailed-table tbody td {
        padding: 0.8rem;
    }

    .d-mobile-none {
        display: none;
    }
}

@media (max-width: 576px) {
    .header-section {
        padding: 1rem;
        text-align: center;
    }

    .customer-info {
        text-align: center;
    }

    .customer-avatar-lg {
        margin: 0 auto 1rem;
    }

    .customer-meta {
        justify-content: center;
    }

    .stats-grid {
        grid-template-columns: 1fr;
    }

    .action-buttons {
        justify-content: center;
        margin-top: 1rem;
    }

    .btn-back, .btn-edit {
        width: 100%;
        justify-content: center;
    }

    .detailed-table {
        display: block;
        overflow-x: auto;
        white-space: nowrap;
    }

    .detailed-table thead {
        display: none;
    }

    .detailed-table tbody tr {
        display: block;
        margin-bottom: 1rem;
        border: 1px solid #e2e8f0;
        border-radius: 10px;
        padding: 1rem;
    }

    .detailed-table tbody td {
        display: block;
        text-align: left;
        padding: 0.5rem;
        border: none;
        position: relative;
    }

    .detailed-table tbody td::before {
        content: attr(data-label);
        font-weight: 600;
        color: #4a5568;
        display: block;
        margin-bottom: 0.2rem;
    }

    .amount-cell {
        text-align: left;
    }
}
</style>
{% endblock %}

{% block content %}
<div class="container py-4">
    <!-- Header Section -->
    <div class="header-section">
        <div class="row align-items-center">
            <div class="col-md-8">
                <div class="d-flex align-items-center">
                    <div class="customer-avatar-lg">
                        {{ customer.name|first|upper }}
                    </div>
                    <div class="customer-info">
                        <h1 class="mb-2">{{ customer.name }}</h1>
                        <div class="customer-meta">
                            <div class="meta-item">
                                <i class="fas fa-phone"></i>
                                <span>{{ customer.phone_number }}</span>
                            </div>
                            {% if customer.birth_date %}
                            <div class="meta-item">
                                <i class="fas fa-birthday-cake"></i>
                                <span>{{ customer.birth_date|date:"d.m.Y" }} ({{ customer.age }} yosh)</span>
                            </div>
                            {% endif %}
                            <div class="meta-item">
                                <i class="fas fa-calendar-plus"></i>
                                <span>Qo'shilgan: {{ customer.created_at|date:"d.m.Y" }}</span>
                            </div>
                            {% if customer.created_by %}
                            <div class="meta-item">
                                <i class="fas fa-user-plus"></i>
                                <span>{{ customer.created_by.get_full_name|default:customer.created_by.username }}</span>
                            </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-4 text-md-end text-center mt-3 mt-md-0">
                <div class="action-buttons">
                    <a href="{% url 'shop:customer_list' %}" class="btn-back" aria-label="Mijozlar ro'yxatiga qaytish">
                        <i class="fas fa-arrow-left me-2"></i>Orqaga
                    </a>
                    {% if can_edit_customer %}
                    <a href="{% url 'shop:customer_edit' customer.pk %}" class="btn-edit" aria-label="Mijoz ma'lumotlarini tahrirlash">
                        <i class="fas fa-edit me-2"></i>Tahrirlash
                    </a>
                    {% else %}
                    <button class="btn-disabled" disabled title="Sizda tahrirlash ruxsati yo'q" aria-label="Tahrirlash ruxsati yo'q">
                        <i class="fas fa-edit me-2"></i>Tahrirlash
                    </button>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- Statistics -->
    <div class="stats-grid">
        <div class="stat-card primary">
            <div class="stat-icon">
                <i class="fas fa-shopping-cart"></i>
            </div>
            <div class="stat-number">{{ total_purchases_count }}</div>
            <div class="stat-label">Jami Xaridlar</div>
        </div>
        <div class="stat-card success">
            <div class="stat-icon">
                <i class="fas fa-dollar-sign"></i>
            </div>
            <div class="stat-number">${{ total_spent_usd|floatformat:0 }}</div>
            <div class="stat-label">Xaridlar ($)</div>
        </div>
        <div class="stat-card warning">
            <div class="stat-icon">
                <i class="fas fa-money-bill-wave"></i>
            </div>
            <div class="stat-number">{{ total_spent_uzs|floatformat:0 }}</div>
            <div class="stat-label">Xaridlar (so'm)</div>
        </div>
        <div class="stat-card danger">
            <div class="stat-icon">
                <i class="fas fa-credit-card"></i>
            </div>
            <div class="stat-number">{{ customer.active_debts_count }}</div>
            <div class="stat-label">Faol Qarzlar</div>
        </div>
    </div>

    <!-- Batafsil Telefon Sotuvlari -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title">
                <i class="fas fa-mobile-alt me-2"></i>Telefon Sotuvlari
            </h5>
            <span class="section-count">{{ detailed_phone_sales|length }}</span>
        </div>

        <div class="table-responsive">
            <table class="table detailed-table">
                <thead>
                    <tr>
                        <th>Telefon Ma'lumotlari</th>
                        <th class="d-mobile-none">Manba</th>
                        <th>Narxlar</th>
                        <th>To'lov Usullari</th>
                        <th class="d-mobile-none">Sana</th>
                    </tr>
                </thead>
                <tbody>
                    {% if detailed_phone_sales %}
                        {% for sale in detailed_phone_sales %}
                        <tr>
                            <td data-label="Telefon Ma'lumotlari">
                                <div>
                                    <div class="phone-model">{{ sale.phone_model }} {{ sale.memory_size }}</div>
                                    <div class="phone-details">
                                        IMEI: {{ sale.imei }} | Holati: {{ sale.condition }}%
                                    </div>
                                    <div class="phone-details">
                                        Sotuvchi: {{ sale.salesman }}
                                    </div>
                                </div>
                            </td>
                            <td data-label="Manba" class="d-mobile-none">
                                <div>
                                    <span class="source-badge">{{ sale.source_type }}</span>
                                    <div class="phone-details mt-1">
                                        {{ sale.supplier }}
                                        {% if sale.external_seller %}
                                            <br>Diler: {{ sale.external_seller }}
                                        {% endif %}
                                        {% if sale.daily_seller %}
                                            <br>Kunlik: {{ sale.daily_seller }}
                                        {% endif %}
                                    </div>
                                    <div class="phone-details">
                                        Qo'shgan: {{ sale.created_by }}
                                    </div>
                                </div>
                            </td>
                            <td data-label="Narxlar" class="amount-cell">
                                <div>
                                    <div class="amount-main text-success">${{ sale.sale_price|floatformat:0 }}</div>
                                    <div class="amount-sub">Tannarx: ${{ sale.cost_price|floatformat:0 }}</div>
                                    <div class="mt-1">
                                        <span class="profit-badge {% if sale.profit > 0 %}profit-positive{% else %}profit-negative{% endif %}">
                                            Foyda: ${{ sale.profit|floatformat:0 }}
                                        </span>
                                    </div>
                                </div>
                            </td>
                            <td data-label="To'lov Usullari">
                                <div class="payment-methods">
                                    {% if sale.payment_methods.cash > 0 %}
                                    <span class="payment-badge payment-cash">Naqd: ${{ sale.payment_methods.cash|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if sale.payment_methods.card > 0 %}
                                    <span class="payment-badge payment-card">Karta: ${{ sale.payment_methods.card|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if sale.payment_methods.credit > 0 %}
                                    <span class="payment-badge payment-credit">Nasiya: ${{ sale.payment_methods.credit|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if sale.payment_methods.debt > 0 %}
                                    <span class="payment-badge payment-debt">Qarz: ${{ sale.payment_methods.debt|floatformat:0 }}</span>
                                    {% endif %}
                                </div>
                            </td>
                            <td data-label="Sana" class="d-mobile-none">
                                <div class="text-center">
                                    <div class="amount-main">{{ sale.sale_date|date:"d.m.Y" }}</div>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    {% else %}
                        <tr>
                            <td colspan="5" class="empty-state">
                                <div class="empty-icon">
                                    <i class="fas fa-mobile-alt"></i>
                                </div>
                                <div class="empty-title">Telefon sotuvi mavjud emas</div>
                                <div class="empty-subtitle">Mijoz hali hech qanday telefon sotib olmagan</div>
                            </td>
                        </tr>
                    {% endif %}
                </tbody>
            </table>
        </div>
    </div>

    <!-- Batafsil Telefon Almashtirishlar -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title">
                <i class="fas fa-exchange-alt me-2"></i>Telefon Almashtirishlar
            </h5>
            <span class="section-count">{{ detailed_phone_exchanges|length }}</span>
        </div>

        <div class="table-responsive">
            <table class="table detailed-table">
                <thead>
                    <tr>
                        <th>Almashtirish Ma'lumotlari</th>
                        <th class="d-mobile-none">Eski Telefon</th>
                        <th>Narx Farqi</th>
                        <th>To'lov Usullari</th>
                        <th class="d-mobile-none">Sana</th>
                    </tr>
                </thead>
                <tbody>
                    {% if detailed_phone_exchanges %}
                        {% for exchange in detailed_phone_exchanges %}
                        <tr>
                            <td data-label="Almashtirish Ma'lumotlari">
                                <div>
                                    <div class="phone-model">{{ exchange.old_phone_model }} → {{ exchange.new_phone_model }}</div>
                                    <div class="phone-details">
                                        Yangi IMEI: {{ exchange.new_phone_imei }}
                                    </div>
                                    <div class="phone-details">
                                        Sotuvchi: {{ exchange.salesman }}
                                    </div>
                                    <div class="phone-details">
                                        <span class="source-badge">{{ exchange.exchange_type }}</span>
                                    </div>
                                </div>
                            </td>
                            <td data-label="Eski Telefon" class="d-mobile-none">
                                <div>
                                    <div class="phone-details">
                                        IMEI: {{ exchange.old_phone_imei|default:"N/A" }}
                                    </div>
                                    <div class="phone-details">
                                        Xotira: {{ exchange.old_phone_memory }}
                                    </div>
                                    <div class="phone-details">
                                        Qabul narxi: ${{ exchange.old_phone_accepted_price|floatformat:0 }}
                                    </div>
                                </div>
                            </td>
                            <td data-label="Narx Farqi" class="amount-cell">
                                <div>
                                    <div class="amount-main text-success">${{ exchange.new_phone_price|floatformat:0 }}</div>
                                    <div class="amount-sub">Eski: ${{ exchange.old_phone_accepted_price|floatformat:0 }}</div>
                                    <div class="mt-1">
                                        <span class="profit-badge {% if exchange.price_difference > 0 %}profit-positive{% else %}profit-negative{% endif %}">
                                            Farq: ${{ exchange.price_difference|floatformat:0 }}
                                        </span>
                                    </div>
                                </div>
                            </td>
                            <td data-label="To'lov Usullari">
                                <div class="payment-methods">
                                    {% if exchange.payment_methods.cash > 0 %}
                                    <span class="payment-badge payment-cash">Naqd: ${{ exchange.payment_methods.cash|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if exchange.payment_methods.card > 0 %}
                                    <span class="payment-badge payment-card">Karta: ${{ exchange.payment_methods.card|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if exchange.payment_methods.credit > 0 %}
                                    <span class="payment-badge payment-credit">Nasiya: ${{ exchange.payment_methods.credit|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if exchange.payment_methods.debt > 0 %}
                                    <span class="payment-badge payment-debt">Qarz: ${{ exchange.payment_methods.debt|floatformat:0 }}</span>
                                    {% endif %}
                                </div>
                            </td>
                            <td data-label="Sana" class="d-mobile-none">
                                <div class="text-center">
                                    <div class="amount-main">{{ exchange.exchange_date|date:"d.m.Y" }}</div>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    {% else %}
                        <tr>
                            <td colspan="5" class="empty-state">
                                <div class="empty-icon">
                                    <i class="fas fa-exchange-alt"></i>
                                </div>
                                <div class="empty-title">Almashtirish mavjud emas</div>
                                <div class="empty-subtitle">Mijoz hali hech qanday telefon almashtirmagan</div>
                            </td>
                        </tr>
                    {% endif %}
                </tbody>
            </table>
        </div>
    </div>

    <!-- Aksessuar Sotuvlari -->
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title">
                <i class="fas fa-headphones me-2"></i>Aksessuar Sotuvlari
            </h5>
            <span class="section-count">{{ accessory_sales.count }}</span>
        </div>

        <div class="table-responsive">
            <table class="table detailed-table">
                <thead>
                    <tr>
                        <th>Aksessuar</th>
                        <th>Soni</th>
                        <th>Narx</th>
                        <th>To'lov Usullari</th>
                        <th class="d-mobile-none">Sana</th>
                    </tr>
                </thead>
                <tbody>
                    {% if accessory_sales %}
                        {% for sale in accessory_sales %}
                        <tr>
                            <td data-label="Aksessuar">
                                <div>
                                    <div class="phone-model">{{ sale.accessory.name }}</div>
                                    <div class="phone-details">
                                        Kod: {{ sale.accessory.code }}
                                    </div>
                                    <div class="phone-details">
                                        Sotuvchi: {{ sale.salesman.get_full_name|default:sale.salesman.username }}
                                    </div>
                                </div>
                            </td>
                            <td data-label="Soni">
                                <span class="badge bg-secondary">{{ sale.quantity }}x</span>
                            </td>
                            <td data-label="Narx" class="amount-cell">
                                <div>
                                    <div class="amount-main text-info">{{ sale.total_price|floatformat:0 }} so'm</div>
                                    <div class="amount-sub">Birlik: {{ sale.unit_price|floatformat:0 }} so'm</div>
                                </div>
                            </td>
                            <td data-label="To'lov Usullari">
                                <div class="payment-methods">
                                    {% if sale.cash_amount > 0 %}
                                    <span class="payment-badge payment-cash">Naqd: {{ sale.cash_amount|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if sale.card_amount > 0 %}
                                    <span class="payment-badge payment-card">Karta: {{ sale.card_amount|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if sale.credit_amount > 0 %}
                                    <span class="payment-badge payment-credit">Nasiya: {{ sale.credit_amount|floatformat:0 }}</span>
                                    {% endif %}
                                    {% if sale.debt_amount > 0 %}
                                    <span class="payment-badge payment-debt">Qarz: {{ sale.debt_amount|floatformat:0 }}</span>
                                    {% endif %}
                                </div>
                            </td>
                            <td data-label="Sana" class="d-mobile-none">
                                <div class="text-center">
                                    <div class="amount-main">{{ sale.sale_date|date:"d.m.Y" }}</div>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    {% else %}
                        <tr>
                            <td colspan="5" class="empty-state">
                                <div class="empty-icon">
                                    <i class="fas fa-headphones"></i>
                                </div>
                                <div class="empty-title">Aksessuar sotuvi mavjud emas</div>
                                <div class="empty-subtitle">Mijoz hali hech qanday aksessuar sotib olmagan</div>
                            </td>
                        </tr>
                    {% endif %}
                </tbody>
            </table>
        </div>
    </div>

    <!-- Faol Qarzlar -->
    {% if active_debts %}
    <div class="section-card">
        <div class="section-header">
            <h5 class="section-title">
                <i class="fas fa-credit-card me-2"></i>Faol Qarzlar
            </h5>
            <span class="section-count">{{ active_debts.count }}</span>
        </div>

        <div class="table-responsive">
            <table class="table detailed-table">
                <thead>
                    <tr>
                        <th>Qarz Turi</th>
                        <th>Qarz Bergan</th>
                        <th>Summa</th>
                        <th>To'langan</th>
                        <th>Qolgan</th>
                        <th class="d-mobile-none">Muddati</th>
                    </tr>
                </thead>
                <tbody>
                    {% for debt in active_debts %}
                    <tr>
                        <td data-label="Qarz Turi">
                            <div class="phone-model">{{ debt.get_debt_type_display }}</div>
                        </td>
                        <td data-label="Qarz Bergan">
                            {{ debt.creditor.get_full_name|default:debt.creditor.username }}
                        </td>
                        <td data-label="Summa" class="amount-cell">
                            <div class="amount-main {% if debt.currency == 'USD' %}text-success{% else %}text-info{% endif %}">
                                {% if debt.currency == 'USD' %}${% endif %}{{ debt.debt_amount|floatformat:0 }}{% if debt.currency == 'UZS' %} so'm{% endif %}
                            </div>
                        </td>
                        <td data-label="To'langan" class="amount-cell">
                            <div class="amount-sub">
                                {% if debt.currency == 'USD' %}${% endif %}{{ debt.paid_amount|floatformat:0 }}{% if debt.currency == 'UZS' %} so'm{% endif %}
                            </div>
                        </td>
                        <td data-label="Qolgan" class="amount-cell">
                            <div class="amount-main text-danger">
                                {% if debt.currency == 'USD' %}${% endif %}{{ debt.remaining_amount|floatformat:0 }}{% if debt.currency == 'UZS' %} so'm{% endif %}
                            </div>
                        </td>
                        <td data-label="Muddati" class="d-mobile-none">
                            {% if debt.due_date %}
                            <div class="text-center">
                                <div class="amount-main">{{ debt.due_date|date:"d.m.Y" }}</div>
                                <div class="amount-sub {% if debt.due_date < today %}text-danger{% endif %}">
                                    {{ debt.due_date|timeuntil }}
                                </div>
                            </div>
                            {% else %}
                            <span class="text-muted">-</span>
                            {% endif %}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %}