{% extends 'base.html' %}

{% block title %}Mijozlar Ro'yxati{% endblock %}
{% block page_title %}Mijozlar Ro'yxati{% endblock %}

{% block content %}
<div class="container py-4">
    <!-- Mijozlar statistikasi -->
    <div class="row g-3 mb-4">
        <div class="col-12 col-md-4">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-body text-center">
                    <h5 class="card-title fw-bold text-success">Jami Mijozlar</h5>
                    <p class="card-text fs-4">{{ total_customers }}</p>
                    {% if is_boss or request.user.userprofile.role == 'seller' %}
                    <a href="{% url 'shop:customer_create' %}" class="btn btn-success btn-sm mt-2">
                        <i class="fas fa-plus me-1"></i> Yangi Mijoz
                    </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- Mijozlar ro'yxati -->
    <div class="card mb-4 shadow-sm">
        <div class="card-body">
            <h5 class="card-title fw-bold mb-3">Mijozlar <span class="badge bg-secondary">{{ total_customers }}</span></h5>
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Ism</th>
                            <th class="d-none d-md-table-cell">Telefon</th>
                            <th>Umumiy xarid</th>
                            <th>Qarz</th>
                            <th>Nima olgan</th>
                            <th class="text-center">Amallar</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% if customer_page_obj %}
                            {% for customer in customer_page_obj %}
                            <tr>
                                <td>{{ customer.name|default:"Noma'lum" }}</td>
                                <td class="d-none d-md-table-cell">{{ customer.phone_number|default:'-' }}</td>
                                <td>{{ customer.total_purchases|floatformat:0 }}$</td>
                                <td>{{ customer.total_debt|floatformat:0 }}$</td>
                                <td>
                                    {% for sale in customer.phone_sales.all|slice:":1" %}
                                        Telefon: {{ sale.phone }}
                                    {% empty %}
                                        {% for sale in customer.accessory_sales.all|slice:":1" %}
                                            Aksessuar: {{ sale.accessory.name }} ({{ sale.quantity }} dona)
                                        {% empty %}
                                            {% for exchange in customer.phone_exchanges.all|slice:":1" %}
                                                Almashtirish: {{ exchange.old_phone_model }} → {{ exchange.new_phone }}
                                            {% empty %}
                                                -
                                            {% endfor %}
                                        {% endfor %}
                                    {% endfor %}
                                </td>
                                <td class="text-center">
                                    <div class="btn-group">
                                        <a href="{% url 'shop:customer_detail' customer.pk %}" class="btn btn-sm btn-info me-1" title="Ko'rish">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        {% if is_boss or request.user.userprofile.role == 'seller' %}
                                        <a href="{% url 'shop:customer_edit' customer.pk %}" class="btn btn-sm btn-primary me-1" title="Tahrirlash">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a href="{% url 'shop:customer_delete' customer.pk %}" class="btn btn-sm btn-danger" title="O'chirish">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                        {% endif %}
                                    </div>
                                </td>
                            </tr>
                            {% endfor %}
                        {% else %}
                            <tr><td colspan="6" class="text-center text-muted">Mijozlar topilmadi.</td></tr>
                        {% endif %}
                    </tbody>
                </table>
            </div>
            <!-- Pagination for Customers -->
            {% if customer_page_obj.has_other_pages %}
            <nav aria-label="Customer pagination">
                <ul class="pagination justify-content-center mt-3">
                    {% if customer_page_obj.has_previous %}
                    <li class="page-item">
                        <a class="page-link" href="?page={{ customer_page_obj.previous_page_number }}" aria-label="Oldingi">
                            <span aria-hidden="true">&laquo;</span>
                        </a>
                    </li>
                    {% endif %}
                    {% for num in customer_page_obj.paginator.page_range %}
                    <li class="page-item {% if customer_page_obj.number == num %}active{% endif %}">
                        <a class="page-link {% if customer_page_obj.number == num %}bg-primary text-white{% endif %}" href="?page={{ num }}">{{ num }}</a>
                    </li>
                    {% endfor %}
                    {% if customer_page_obj.has_next %}
                    <li class="page-item">
                        <a class="page-link" href="?page={{ customer_page_obj.next_page_number }}" aria-label="Keyingi">
                            <span aria-hidden="true">&raquo;</span>
                        </a>
                    </li>
                    {% endif %}
                </ul>
            </nav>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}