<!-- templates/inventory/supplier_payment_detail.html -->
{% extends 'base.html' %}
{% load static %}

{% block title %}To'lov Tafsilotlari - #{{ payment.id }}{% endblock %}

{% block content %}
<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <!-- Header -->
            <div class="card mb-4">
                <div class="card-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h4 class="mb-1">
                                <i class="fas fa-file-invoice-dollar me-2"></i>
                                To'lov Tafsilotlari #{{ payment.id }}
                            </h4>
                            <small class="text-muted">
                                Taminotchi: <strong>{{ payment.supplier.name }}</strong>
                            </small>
                        </div>
                        <div class="d-flex gap-2">
                            <button onclick="window.print()" class="btn btn-outline-primary">
                                <i class="fas fa-print me-1"></i>Chop etish
                            </button>
                            <a href="{% url 'inventory:supplier_detail' payment.supplier.id %}"
                               class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-1"></i>Ortga
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- To'lov Ma'lumotlari -->
            <div class="row mb-4">
                <div class="col-md-3">
                    <div class="card border-success">
                        <div class="card-body text-center">
                            <h6 class="text-muted mb-2">To'lov Summasi</h6>
                            <h3 class="mb-0 text-success">
                                ${{ payment.amount|floatformat:2 }}
                            </h3>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card border-info">
                        <div class="card-body text-center">
                            <h6 class="text-muted mb-2">To'lov Turi</h6>
                            <h5 class="mb-0">
                                <span class="badge bg-{% if payment.payment_type == 'general' %}info{% else %}warning{% endif %}">
                                    {{ payment.get_payment_type_display }}
                                </span>
                            </h5>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card border-primary">
                        <div class="card-body text-center">
                            <h6 class="text-muted mb-2">Telefonlar Soni</h6>
                            <h3 class="mb-0 text-primary">
                                {{ payment.details.count }}
                            </h3>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card border-secondary">
                        <div class="card-body text-center">
                            <h6 class="text-muted mb-2">To'lov Sanasi</h6>
                            <h5 class="mb-0">
                                {{ payment.payment_date|date:"d.m.Y" }}
                            </h5>
                            <small class="text-muted">{{ payment.created_at|date:"H:i" }}</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Qo'shimcha Ma'lumotlar -->
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h6 class="mb-0">
                        <i class="fas fa-info-circle me-2"></i>
                        Qo'shimcha Ma'lumotlar
                    </h6>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-4">
                            <p class="mb-2">
                                <strong>Taminotchi:</strong><br>
                                <a href="{% url 'inventory:supplier_detail' payment.supplier.id %}"
                                   class="text-decoration-none">
                                    {{ payment.supplier.name }}
                                </a>
                            </p>
                        </div>
                        <div class="col-md-4">
                            <p class="mb-2">
                                <strong>Amalga oshirgan:</strong><br>
                                {{ payment.created_by.username }}
                            </p>
                        </div>
                        <div class="col-md-4">
                            <p class="mb-2">
                                <strong>Yaratilgan vaqt:</strong><br>
                                {{ payment.created_at|date:"d.m.Y H:i" }}
                            </p>
                        </div>
                    </div>
                    {% if payment.notes %}
                    <div class="row mt-3">
                        <div class="col-12">
                            <div class="alert alert-info mb-0">
                                <strong>Izoh:</strong><br>
                                {{ payment.notes }}
                            </div>
                        </div>
                    </div>
                    {% endif %}
                </div>
            </div>

            <!-- Telefonlar Tafsilotlari -->
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-mobile-alt me-2"></i>
                        To'lov Tarqatish Tafsilotlari
                        <span class="badge bg-light text-dark ms-2">{{ payment.details.count }} ta telefon</span>
                    </h5>
                </div>
                <div class="card-body p-0">
                    {% if payment.details.exists %}
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th width="50">#</th>
                                    <th>Telefon</th>
                                    <th>IMEI</th>
                                    <th>Tan Narx</th>
                                    <th class="text-end">Oldingi Qarz</th>
                                    <th class="text-end">To'langan</th>
                                    <th class="text-end">Yangi Qarz</th>
                                    <th class="text-center">Holat</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for detail in payment.details.all %}
                                <tr>
                                    <td>{{ forloop.counter }}</td>
                                    <td>
                                        <strong>{{ detail.phone.phone_model }}</strong><br>
                                        <small class="text-muted">{{ detail.phone.memory_size }}</small>
                                    </td>
                                    <td>
                                        <small class="font-monospace">{{ detail.phone.imei }}</small>
                                    </td>
                                    <td>
                                        ${{ detail.phone.cost_price|floatformat:2 }}
                                    </td>
                                    <td class="text-end text-danger">
                                        ${{ detail.previous_balance|floatformat:2 }}
                                    </td>
                                    <td class="text-end text-success">
                                        <strong>-${{ detail.amount|floatformat:2 }}</strong>
                                    </td>
                                    <td class="text-end">
                                        {% if detail.new_balance > 0 %}
                                            <span class="text-warning">
                                                ${{ detail.new_balance|floatformat:2 }}
                                            </span>
                                        {% else %}
                                            <span class="text-success">
                                                <i class="fas fa-check-circle me-1"></i>$0.00
                                            </span>
                                        {% endif %}
                                    </td>
                                    <td class="text-center">
                                        {% if detail.new_balance > 0 %}
                                            {% if detail.new_balance < detail.previous_balance %}
                                                <span class="badge bg-warning">Qisman</span>
                                            {% else %}
                                                <span class="badge bg-danger">Qarz</span>
                                            {% endif %}
                                        {% else %}
                                            <span class="badge bg-success">To'landi</span>
                                        {% endif %}
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                            <tfoot class="table-light">
                                <tr>
                                    <th colspan="5" class="text-end">Jami To'langan:</th>
                                    <th class="text-end text-success">
                                        <h5 class="mb-0">-${{ payment.amount|floatformat:2 }}</h5>
                                    </th>
                                    <th colspan="2"></th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    {% else %}
                    <div class="text-center py-5">
                        <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                        <h6 class="text-muted">Tafsilotlar topilmadi</h6>
                    </div>
                    {% endif %}
                </div>
            </div>

            <!-- To'lov Tarixi Havolasi -->
            <div class="card mt-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="mb-1">Taminotchi sahifasiga o'tish</h6>
                            <small class="text-muted">
                                Barcha to'lovlar va qarzlar ro'yxatini ko'rish
                            </small>
                        </div>
                        <a href="{% url 'inventory:supplier_detail' payment.supplier.id %}"
                           class="btn btn-primary">
                            <i class="fas fa-arrow-right me-1"></i>
                            {{ payment.supplier.name }}
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
@media print {
    .btn, .card-header .d-flex .btn-group {
        display: none !important;
    }

    .card {
        border: 1px solid #dee2e6 !important;
        box-shadow: none !important;
    }

    body {
        background: white !important;
    }
}

.table td, .table th {
    vertical-align: middle;
}

.font-monospace {
    font-family: 'Courier New', Courier, monospace;
    font-size: 0.9em;
}
</style>
{% endblock %}



<!-- Header qismiga qo'shing -->
<div class="d-flex gap-2">
    {% if request.user.userprofile.role in 'boss,finance' %}
    <a href="{% url 'inventory:supplier_payment_update' payment.id %}"
       class="btn btn-primary btn-sm">
        <i class="fas fa-edit me-1"></i>Tahrirlash
    </a>
    <button type="button"
            class="btn btn-danger btn-sm"
            data-bs-toggle="modal"
            data-bs-target="#deleteModal">
        <i class="fas fa-trash me-1"></i>O'chirish
    </button>
    {% endif %}
    <a href="{% url 'inventory:supplier_detail' payment.supplier.id %}"
       class="btn btn-outline-secondary btn-sm">
        <i class="fas fa-arrow-left me-1"></i>Ortga
    </a>
</div>

<!-- Modal (sahifa pastida) -->
<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header border-0">
                <h5 class="modal-title">
                    <i class="fas fa-exclamation-triangle text-danger me-2"></i>
                    To'lovni O'chirish
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Ushbu to'lovni o'chirmoqchimisiz?</p>
                <p class="mb-0"><strong>Summa:</strong> <span class="text-danger">${{ payment.amount|floatformat:2 }}</span></p>
            </div>
            <div class="modal-footer border-0">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Bekor Qilish</button>
                <a href="{% url 'inventory:supplier_payment_delete' payment.id %}" class="btn btn-danger">O'chirish</a>
            </div>
        </div>
    </div>
</div>