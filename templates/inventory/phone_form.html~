{% extends 'base.html' %}
{% load static %}

{% block title %}{% if is_edit %}Telefon Tahrirlash{% else %}Yangi Telefon{% endif %}{% endblock %}

{% block content %}
<div class="container-fluid py-4">
    <!-- Messages -->
    {% if messages %}
        {% for message in messages %}
            <div class="alert alert-{{ message.tags }} alert-dismissible fade show" role="alert">
                <i class="fas fa-{% if message.tags == 'success' %}check-circle{% elif message.tags == 'error' %}exclamation-triangle{% else %}info-circle{% endif %} me-2"></i>
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
    {% endif %}

    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h4 class="mb-0">
                                <i class="fas fa-mobile-alt me-2"></i>
                                {% if is_edit %}Telefon Tahrirlash{% else %}Yangi Telefon Qo'shish{% endif %}
                            </h4>
                            <small class="text-muted">
                                {% if is_edit %}
                                    IMEI: {{ form.instance.imei }}
                                    {% if form.instance.created_by %}
                                        | Qo'shgan: {{ form.instance.created_by.get_full_name|default:form.instance.created_by.username }}
                                    {% endif %}
                                {% else %}
                                    Barcha majburiy maydonlarni to'ldiring
                                {% endif %}
                            </small>
                        </div>
                        <a href="{% url 'inventory:phone_list' %}" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-1"></i>Ortga
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <form method="post" enctype="multipart/form-data" novalidate>
        {% csrf_token %}

        <!-- Non-field Errors -->
        {% if form.non_field_errors %}
            <div class="alert alert-danger alert-dismissible fade show">
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                <ul class="mb-0">
                    {% for error in form.non_field_errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        <div class="row g-4">
            <!-- Left Column (8/12) -->
            <div class="col-lg-8">
                <!-- Asosiy Ma'lumotlar -->
                <div class="card mb-4">
                    <div class="card-header bg-primary text-white">
                        <h6 class="mb-0">
                            <i class="fas fa-info-circle me-2"></i>
                            Asosiy Ma'lumotlar
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <!-- Do'kon -->
                            <div class="col-md-6">
                                <label for="{{ form.shop.id_for_label }}" class="form-label fw-bold">
                                    Do'kon <span class="text-danger">*</span>
                                </label>
                                {{ form.shop }}
                                {% if form.shop.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.shop.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            </div>

                            <!-- Sana -->
                            <div class="col-md-6">
                                <label for="{{ form.created_at.id_for_label }}" class="form-label fw-bold">
                                    Qo'shilgan Sana <span class="text-danger">*</span>
                                </label>
                                {{ form.created_at }}
                                {% if form.created_at.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.created_at.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div class="form-text">Telefon qo'shilgan sanani tanlang</div>
                            </div>

                            <!-- Model -->
                            <div class="col-md-6">
                                <label for="{{ form.phone_model.id_for_label }}" class="form-label fw-bold">
                                    Telefon Modeli <span class="text-danger">*</span>
                                </label>
                                {{ form.phone_model }}
                                {% if form.phone_model.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.phone_model.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            </div>

                            <!-- Xotira -->
                            <div class="col-md-6">
                                <label for="{{ form.memory_size.id_for_label }}" class="form-label fw-bold">
                                    Xotira Hajmi <span class="text-danger">*</span>
                                </label>
                                {{ form.memory_size }}
                                {% if form.memory_size.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.memory_size.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            </div>

                            <!-- IMEI - ✅ YANGI CHECK FUNKSIYASI -->
                            <div class="col-md-6">
                                <label for="{{ form.imei.id_for_label }}" class="form-label fw-bold">
                                    IMEI Raqami <span class="text-danger">*</span>
                                </label>
                                {{ form.imei }}
                                <!-- ✅ IMEI Tekshirish natijasi -->
                                <div id="imei_check_result" class="mt-2"></div>
                                {% if form.imei.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.imei.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div class="form-text">15 raqamli noyob IMEI raqami (majburiy)</div>
                            </div>

                            <!-- Holat % -->
                            <div class="col-md-6">
                                <label for="{{ form.condition_percentage.id_for_label }}" class="form-label fw-bold">
                                    Telefon Holati (%) <span class="text-danger">*</span>
                                </label>
                                {{ form.condition_percentage }}
                                {% if form.condition_percentage.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.condition_percentage.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div class="form-text">1 dan 100 gacha (100 = yangi)</div>
                            </div>

                            <!-- Status -->
                            <div class="col-md-12">
                                <label for="{{ form.status.id_for_label }}" class="form-label fw-bold">
                                    Telefon Holati
                                </label>
                                {{ form.status }}
                                {% if form.status.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.status.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            </div>

                            <!-- Izoh -->
                            <div class="col-md-12">
                                <label for="{{ form.note.id_for_label }}" class="form-label fw-bold">
                                    Izoh
                                </label>
                                {{ form.note }}
                                {% if form.note.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.note.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div class="form-text">Telefon haqida qo'shimcha ma'lumot (ixtiyoriy)</div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Narx Ma'lumotlari -->
                <div class="card mb-4">
                    <div class="card-header bg-success text-white">
                        <h6 class="mb-0">
                            <i class="fas fa-dollar-sign me-2"></i>
                            Narx Ma'lumotlari
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <!-- Sotib olingan narx -->
                            <div class="col-md-6">
                                <label for="{{ form.purchase_price.id_for_label }}" class="form-label fw-bold">
                                    Sotib Olingan Narx ($) <span class="text-danger">*</span>
                                </label>
                                {{ form.purchase_price }}
                                {% if form.purchase_price.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.purchase_price.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div id="daily_payment_info" style="display:none;">
                                    <small class="text-success">
                                        <i class="fas fa-info-circle"></i> Bu summa kunlik sotuvchiga naqd to'lanadi
                                    </small>
                                </div>
                            </div>

                            <!-- IMEI xarajat -->
                            <div class="col-md-6">
                                <label for="{{ form.imei_cost.id_for_label }}" class="form-label fw-bold">
                                    IMEI Xarajati ($)
                                </label>
                                {{ form.imei_cost }}
                                {% if form.imei_cost.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.imei_cost.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div class="form-text">IMEI bilan bog'liq xarajatlar (ixtiyoriy)</div>
                            </div>

                            <!-- Ta'mirlash xarajati -->
                            <div class="col-md-6">
                                <label for="{{ form.repair_cost.id_for_label }}" class="form-label fw-bold">
                                    Ta'mirlash Xarajati ($)
                                </label>
                                {{ form.repair_cost }}
                                {% if form.repair_cost.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.repair_cost.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div class="form-text">Ta'mirlash uchun sarflangan mablag' (ixtiyoriy)</div>
                            </div>

                            <!-- Sotish narxi -->
                            <div class="col-md-6">
                                <label for="{{ form.sale_price.id_for_label }}" class="form-label fw-bold">
                                    Sotish Narxi ($)
                                </label>
                                {{ form.sale_price }}
                                {% if form.sale_price.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.sale_price.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div class="form-text">Rejalashtirilgan sotish narxi (ixtiyoriy)</div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Manba Ma'lumotlari -->
                <div class="card mb-4">
                    <div class="card-header bg-info text-white">
                        <h6 class="mb-0">
                            <i class="fas fa-source me-2"></i>
                            Manba Ma'lumotlari
                        </h6>
                    </div>
                    <div class="card-body">
                        <!-- Manba turi -->
                        <div class="mb-3">
                            <label for="{{ form.source_type.id_for_label }}" class="form-label fw-bold">
                                Manba Turi <span class="text-danger">*</span>
                            </label>
                            {{ form.source_type }}
                            {% if form.source_type.errors %}
                                <div class="text-danger small mt-1">
                                    {% for error in form.source_type.errors %}
                                        <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                    {% endfor %}
                                </div>
                            {% endif %}
                            <div class="form-text">Telefon qayerdan olingan</div>
                        </div>

                        <!-- Taminotchi Field -->
                        <div id="supplier_field" style="display: none;">
                            <label for="{{ form.supplier.id_for_label }}" class="form-label fw-bold">
                                Taminotchi <span class="text-danger">*</span>
                            </label>
                            {{ form.supplier }}
                            {% if form.supplier.errors %}
                                <div class="text-danger small mt-1">
                                    {% for error in form.supplier.errors %}
                                        <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                    {% endfor %}
                                </div>
                            {% endif %}

                            <!-- Qarz checkbox (faqat yangi telefon va taminotchi uchun) -->
                            {% if not is_edit %}
                            <div class="mt-3">
                                <div class="card border-warning">
                                    <div class="card-body">
                                        <div class="form-check">
                                            {{ form.is_debt }}
                                            <label class="form-check-label fw-bold" for="{{ form.is_debt.id_for_label }}">
                                                <i class="fas fa-exclamation-triangle text-warning me-1"></i>
                                                Qarzga olish
                                            </label>
                                        </div>
                                        <small class="text-muted d-block mt-2">
                                            <i class="fas fa-info-circle"></i>
                                            Belgilansa, telefon qarzga olingan hisoblanadi va taminotchi qarziga qo'shiladi
                                        </small>
                                    </div>
                                </div>
                            </div>
                            {% endif %}
                        </div>

                        <!-- Tashqi Sotuvchi Field -->
                        <div id="external_seller_field" style="display: none;">
                            <div class="mb-3">
                                <label for="{{ form.external_seller.id_for_label }}" class="form-label fw-bold">
                                    Mavjud Tashqi Sotuvchi
                                </label>
                                {{ form.external_seller }}
                                {% if form.external_seller.errors %}
                                    <div class="text-danger small mt-1">
                                        {% for error in form.external_seller.errors %}
                                            <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                                <div class="form-text">Mavjud sotuvchilardan tanlang yoki pastda yangi yarating</div>
                            </div>

                            <div class="card border-primary">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0">Yangi Tashqi Sotuvchi Qo'shish</h6>
                                </div>
                                <div class="card-body">
                                    <div class="row g-2">
                                        <div class="col-md-6">
                                            <label for="{{ form.external_seller_name.id_for_label }}" class="form-label">
                                                Sotuvchi Ismi
                                            </label>
                                            {{ form.external_seller_name }}
                                            {% if form.external_seller_name.errors %}
                                                <div class="text-danger small mt-1">
                                                    {% for error in form.external_seller_name.errors %}
                                                        <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                                    {% endfor %}
                                                </div>
                                            {% endif %}
                                        </div>
                                        <div class="col-md-6">
                                            <label for="{{ form.external_seller_phone.id_for_label }}" class="form-label">
                                                Telefon Raqami
                                            </label>
                                            {{ form.external_seller_phone }}
                                            <div id="external_phone_check" class="mt-1"></div>
                                            {% if form.external_seller_phone.errors %}
                                                <div class="text-danger small mt-1">
                                                    {% for error in form.external_seller_phone.errors %}
                                                        <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                                    {% endfor %}
                                                </div>
                                            {% endif %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Kunlik Sotuvchi Field -->
                        <div id="daily_seller_field" style="display: none;">
                            <div class="alert alert-info">
                                <i class="fas fa-info-circle me-2"></i>
                                <strong>Eslatma:</strong> Kunlik sotuvchiga sotib olingan narx ($) naqd to'lanadi
                            </div>

                            <div class="card border-success">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0">Kunlik Sotuvchi Ma'lumotlari</h6>
                                </div>
                                <div class="card-body">
                                    <div class="row g-3">
                                        <div class="col-md-6">
                                            <label for="{{ form.daily_seller_name.id_for_label }}" class="form-label fw-bold">
                                                Sotuvchi Ismi <span class="text-danger">*</span>
                                            </label>
                                            {{ form.daily_seller_name }}
                                            {% if form.daily_seller_name.errors %}
                                                <div class="text-danger small mt-1">
                                                    {% for error in form.daily_seller_name.errors %}
                                                        <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                                    {% endfor %}
                                                </div>
                                            {% endif %}
                                        </div>
                                        <div class="col-md-6">
                                            <label for="{{ form.daily_seller_phone.id_for_label }}" class="form-label fw-bold">
                                                Telefon Raqami <span class="text-danger">*</span>
                                            </label>
                                            {{ form.daily_seller_phone }}
                                            <div id="daily_phone_check" class="mt-1"></div>
                                            {% if form.daily_seller_phone.errors %}
                                                <div class="text-danger small mt-1">
                                                    {% for error in form.daily_seller_phone.errors %}
                                                        <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                                    {% endfor %}
                                                </div>
                                            {% endif %}
                                        </div>
                                    </div>
                                    <div class="mt-2">
                                        <small class="text-muted">
                                            <i class="fas fa-lightbulb"></i>
                                            Telefon raqami mavjud bo'lsa, ma'lumotlari avtomatik yangilanadi
                                        </small>
                                    </div>
                                </div>
                            </div>

                            {{ form.daily_seller }}
                            {{ form.daily_payment_amount }}
                        </div>

                        <!-- Almashtirish Field -->
                        <div id="exchange_field" style="display: none;">
                            <div class="row g-3">
                                <div class="col-md-4">
                                    <label for="{{ form.exchange_value.id_for_label }}" class="form-label fw-bold">
                                        Almashtirish Qiymati ($)
                                    </label>
                                    {{ form.exchange_value }}
                                    {% if form.exchange_value.errors %}
                                        <div class="text-danger small mt-1">
                                            {% for error in form.exchange_value.errors %}
                                                <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                            {% endfor %}
                                        </div>
                                    {% endif %}
                                    <div class="form-text">Almashtirilgan telefon qiymati</div>
                                </div>
                                <div class="col-md-4">
                                    <label for="{{ form.original_owner_name.id_for_label }}" class="form-label fw-bold">
                                        Asl Egasi <span class="text-danger">*</span>
                                    </label>
                                    {{ form.original_owner_name }}
                                    {% if form.original_owner_name.errors %}
                                        <div class="text-danger small mt-1">
                                            {% for error in form.original_owner_name.errors %}
                                                <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                            {% endfor %}
                                        </div>
                                    {% endif %}
                                </div>
                                <div class="col-md-4">
                                    <label for="{{ form.original_owner_phone.id_for_label }}" class="form-label fw-bold">
                                        Telefon Raqami <span class="text-danger">*</span>
                                    </label>
                                    {{ form.original_owner_phone }}
                                    {% if form.original_owner_phone.errors %}
                                        <div class="text-danger small mt-1">
                                            {% for error in form.original_owner_phone.errors %}
                                                <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                            {% endfor %}
                                        </div>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Right Column (4/12) -->
            <div class="col-lg-4">
                <!-- Rasm - ✅ YANGILANGAN -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h6 class="mb-0">
                            <i class="fas fa-image me-2"></i>
                            Telefon Rasmi
                        </h6>
                    </div>
                    <div class="card-body text-center">
                        <!-- ✅ Rasmni ko'rsatish -->
                        <div id="image_preview_container">
                            {% if form.instance.image %}
                                <img src="{{ form.instance.image.url }}" id="image_preview" class="img-fluid rounded mb-3" style="max-height: 300px;" alt="Telefon rasmi">
                            {% else %}
                                <div id="image_placeholder" class="bg-light rounded d-flex align-items-center justify-content-center mb-3" style="height: 300px;">
                                    <i class="fas fa-mobile-alt fa-4x text-muted"></i>
                                </div>
                            {% endif %}
                        </div>

                        {{ form.image }}
                        {% if form.image.errors %}
                            <div class="text-danger small mt-2">
                                {% for error in form.image.errors %}
                                    <i class="fas fa-exclamation-circle me-1"></i>{{ error }}
                                {% endfor %}
                            </div>
                        {% endif %}
                        <div class="form-text mt-2">Telefon rasmini yuklang (ixtiyoriy)</div>
                    </div>
                </div>

                <!-- Tugmalar -->
                <div class="card">
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-success btn-lg">
                                <i class="fas fa-save me-2"></i>
                                {% if is_edit %}Yangilash{% else %}Saqlash{% endif %}
                            </button>
                            <a href="{% url 'inventory:phone_list' %}" class="btn btn-secondary">
                                <i class="fas fa-times me-2"></i>
                                Bekor Qilish
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Qo'shimcha ma'lumot (faqat tahrirlashda) -->
                {% if is_edit %}
                <div class="card mt-4">
                    <div class="card-header">
                        <h6 class="mb-0">
                            <i class="fas fa-info-circle me-2"></i>
                            Qo'shimcha Ma'lumot
                        </h6>
                    </div>
                    <div class="card-body">
                        <small class="text-muted d-block mb-2">Yaratilgan sana:</small>
                        <p class="mb-2">{{ form.instance.created_at|date:"d.m.Y" }}</p>

                        {% if form.instance.created_by %}
                        <small class="text-muted d-block mb-2">Qo'shgan:</small>
                        <p class="mb-2">{{ form.instance.created_by.get_full_name|default:form.instance.created_by.username }}</p>
                        {% endif %}

                        {% if form.instance.updated_at %}
                        <small class="text-muted d-block mb-2">Yangilangan:</small>
                        <p class="mb-0">{{ form.instance.updated_at|date:"d.m.Y" }}</p>
                        {% endif %}
                    </div>
                </div>
                {% endif %}
            </div>
        </div>
    </form>
</div>

<style>
.form-label.fw-bold {
    font-weight: 600;
    margin-bottom: 0.5rem;
}

.card-header h6 {
    font-weight: 600;
}

.form-control:focus,
.form-select:focus {
    border-color: #0d6efd;
    box-shadow: 0 0 0 0.2rem rgba(13, 110, 253, 0.25);
}

.is-invalid {
    border-color: #dc3545 !important;
}

#external_phone_check .alert,
#daily_phone_check .alert {
    margin-bottom: 0;
    font-size: 0.875rem;
}

/* ✅ IMEI Check Styles */
#imei_check_result .alert {
    margin-bottom: 0;
    padding: 0.5rem 0.75rem;
    font-size: 0.875rem;
}

#imei_check_result .alert-danger {
    background-color: #fee;
    border-color: #f5c6cb;
    color: #721c24;
}

#imei_check_result .alert-success {
    background-color: #d4edda;
    border-color: #c3e6cb;
    color: #155724;
}

/* ✅ Image Preview Styles */
#image_preview {
    max-height: 300px;
    object-fit: contain;
}

#image_placeholder {
    transition: all 0.3s ease;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const sourceType = document.getElementById('id_source_type');
    const supplierField = document.getElementById('supplier_field');
    const externalSellerField = document.getElementById('external_seller_field');
    const dailySellerField = document.getElementById('daily_seller_field');
    const exchangeField = document.getElementById('exchange_field');
    const dailyPaymentInfo = document.getElementById('daily_payment_info');

    const externalSellerSelect = document.getElementById('id_external_seller');
    const externalSellerName = document.getElementById('id_external_seller_name');
    const externalSellerPhone = document.getElementById('id_external_seller_phone');
    const externalPhoneCheck = document.getElementById('external_phone_check');

    const dailySellerName = document.getElementById('id_daily_seller_name');
    const dailySellerPhone = document.getElementById('id_daily_seller_phone');
    const dailyPhoneCheck = document.getElementById('daily_phone_check');

    // ✅ IMEI CHECK
    const imeiInput = document.getElementById('id_imei');
    const im