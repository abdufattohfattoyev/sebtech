<!-- templates/inventory/supplier_detail.html -->
{% extends 'base.html' %}
{% load static %}

{% block title %}{{ supplier.name }} - Taminotchi{% endblock %}

{% block content %}
<div class="container-fluid py-4">
    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h4 class="mb-1">
                                <i class="fas fa-truck me-2"></i>{{ supplier.name }}
                            </h4>
                            <small class="text-muted">
                                <i class="fas fa-phone me-1"></i>{{ supplier.phone_number }}
                            </small>
                        </div>
                        <div class="d-flex gap-2">
                            {% if can_edit %}
                            <a href="{% url 'inventory:supplier_update' supplier.id %}"
                               class="btn btn-outline-primary">
                                <i class="fas fa-edit me-1"></i>Tahrirlash
                            </a>
                            {% endif %}
                            <a href="{% url 'inventory:supplier_list' %}"
                               class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-1"></i>Ortga
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Qarz Statistikasi -->
    <div class="row mb-4">
        <div class="col-md-3">
            <div class="card border-primary">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-2">Umumiy Qarz</h6>
                    <h3 class="mb-0 text-primary">
                        ${{ supplier.total_debt|floatformat:2 }}
                    </h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-success">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-2">To'langan</h6>
                    <h3 class="mb-0 text-success">
                        ${{ supplier.total_paid|floatformat:2 }}
                    </h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-{% if balance > 0 %}warning{% else %}secondary{% endif %}">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-2">Qoldiq</h6>
                    <h3 class="mb-0 text-{% if balance > 0 %}warning{% else %}secondary{% endif %}">
                        ${{ balance|floatformat:2 }}
                    </h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-info">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-2">Jami Telefonlar</h6>
                    <h3 class="mb-0 text-info">
                        {{ total_phones }}
                    </h3>
                </div>
            </div>
        </div>
    </div>

    <!-- To'lov Tugmasi -->
    {% if balance > 0 and can_edit %}
    <div class="row mb-4">
        <div class="col-12">
            <div class="alert alert-warning d-flex justify-content-between align-items-center">
                <div>
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <strong>Qarz mavjud:</strong> Bu taminotchida ${{ balance|floatformat:2 }} qarz bor
                </div>
                <a href="{% url 'inventory:supplier_payment_create' supplier.id %}"
                   class="btn btn-success">
                    <i class="fas fa-money-bill-wave me-1"></i>To'lov Qilish
                </a>
            </div>
        </div>
    </div>
    {% endif %}

    <div class="row">
        <!-- Qarzda turgan telefonlar -->
        <div class="col-md-6 mb-4">
            <div class="card">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0">
                        <i class="fas fa-mobile-alt me-2"></i>
                        Qarzda Turgan Telefonlar
                        <span class="badge bg-dark ms-2">{{ debt_phones.count }}</span>
                    </h5>
                </div>
                <div class="card-body p-0">
                    {% if debt_phones %}
                    <div class="table-responsive" style="max-height: 500px; overflow-y: auto;">
                        <table class="table table-hover table-sm mb-0">
                            <thead class="sticky-top bg-light">
                                <tr>
                                    <th>#</th>
                                    <th>Model</th>
                                    <th>IMEI</th>
                                    <th>Tan narx</th>
                                    <th>To'langan</th>
                                    <th>Qarz</th>
                                    <th>Sana</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for phone in debt_phones %}
                                <tr>
                                    <td>{{ forloop.counter }}</td>
                                    <td>
                                        <strong>{{ phone.phone_model }}</strong><br>
                                        <small class="text-muted">{{ phone.memory_size }}</small>
                                    </td>
                                    <td>
                                        <small class="font-monospace">{{ phone.imei }}</small>
                                    </td>
                                    <td>${{ phone.cost_price|floatformat:2 }}</td>
                                    <td class="text-success">
                                        ${{ phone.paid_amount|floatformat:2 }}
                                    </td>
                                    <td class="text-danger">
                                        <strong>${{ phone.debt_balance|floatformat:2 }}</strong>
                                    </td>
                                    <td>
                                        <small>{{ phone.created_at|date:"d.m.Y" }}</small>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                            <tfoot class="table-light">
                                <tr>
                                    <th colspan="5" class="text-end">Jami Qarz:</th>
                                    <th class="text-danger">
                                        ${% widthratio debt_phones 1 1 as total %}
                                        {{ balance|floatformat:2 }}
                                    </th>
                                    <th></th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    {% else %}
                    <div class="text-center py-5">
                        <i class="fas fa-check-circle fa-3x text-success mb-3"></i>
                        <h6 class="text-muted">Qarzda telefon yo'q</h6>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- To'langan telefonlar -->
        <div class="col-md-6 mb-4">
            <div class="card">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-check-circle me-2"></i>
                        To'langan Telefonlar
                        <span class="badge bg-light text-dark ms-2">{{ paid_phones.count }}</span>
                    </h5>
                </div>
                <div class="card-body p-0">
                    {% if paid_phones %}
                    <div class="table-responsive" style="max-height: 500px; overflow-y: auto;">
                        <table class="table table-hover table-sm mb-0">
                            <thead class="sticky-top bg-light">
                                <tr>
                                    <th>#</th>
                                    <th>Model</th>
                                    <th>IMEI</th>
                                    <th>Tan narx</th>
                                    <th>Sana</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for phone in paid_phones|slice:":20" %}
                                <tr>
                                    <td>{{ forloop.counter }}</td>
                                    <td>
                                        <strong>{{ phone.phone_model }}</strong><br>
                                        <small class="text-muted">{{ phone.memory_size }}</small>
                                    </td>
                                    <td>
                                        <small class="font-monospace">{{ phone.imei }}</small>
                                    </td>
                                    <td>${{ phone.cost_price|floatformat:2 }}</td>
                                    <td>
                                        <small>{{ phone.created_at|date:"d.m.Y" }}</small>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    {% if paid_phones.count > 20 %}
                    <div class="card-footer text-center">
                        <small class="text-muted">
                            Ko'rsatilgan: 20 / {{ paid_phones.count }}
                        </small>
                    </div>
                    {% endif %}
                    {% else %}
                    <div class="text-center py-5">
                        <i class="fas fa-mobile-alt fa-3x text-muted mb-3"></i>
                        <h6 class="text-muted">To'langan telefon yo'q</h6>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- To'lovlar Tarixi -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-history me-2"></i>
                        To'lovlar Tarixi
                        <span class="badge bg-primary ms-2">{{ payments.count }}</span>
                    </h5>
                </div>
                <div class="card-body p-0">
                    {% if payments %}
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>#</th>
                                    <th>Sana</th>
                                    <th>Summa</th>
                                    <th>Turi</th>
                                    <th>Telefonlar</th>
                                    <th>Amalga oshirgan</th>
                                    <th>Izoh</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for payment in payments %}
                                <tr>
                                    <td>{{ forloop.counter }}</td>
                                    <td>
                                        <strong>{{ payment.payment_date|date:"d.m.Y" }}</strong><br>
                                        <small class="text-muted">{{ payment.created_at|date:"H:i" }}</small>
                                    </td>
                                    <td class="text-success">
                                        <strong>${{ payment.amount|floatformat:2 }}</strong>
                                    </td>
                                    <td>
                                        <span class="badge bg-{% if payment.payment_type == 'general' %}info{% else %}warning{% endif %}">
                                            {{ payment.get_payment_type_display }}
                                        </span>
                                    </td>
                                    <td>
                                        <span class="badge bg-secondary">
                                            {{ payment.details.count }} ta
                                        </span>
                                    </td>
                                    <td>
                                        <small>{{ payment.created_by.username }}</small>
                                    </td>
                                    <td>
                                        {% if payment.notes %}
                                            <small class="text-muted">{{ payment.notes|truncatewords:5 }}</small>
                                        {% else %}
                                            <small class="text-muted">-</small>
                                        {% endif %}
                                    </td>
                                    <td>
                                        <a href="{% url 'inventory:supplier_payment_detail' payment.id %}"
                                           class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    {% else %}
                    <div class="text-center py-5">
                        <i class="fas fa-file-invoice-dollar fa-3x text-muted mb-3"></i>
                        <h6 class="text-muted">To'lovlar yo'q</h6>
                        <p class="text-muted">Hozircha hech qanday to'lov amalga oshirilmagan</p>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.sticky-top {
    position: sticky;
    top: 0;
    z-index: 10;
}
</style>
{% endblock %}