{% extends 'base.html' %}
{% load static humanize %}

{% block title %}{{ supplier.name }}{% endblock %}

{% block content %}
<div class="container-fluid py-4">
    <!-- 📌 HEADER -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h3 class="mb-1">
                <i class="fas fa-truck text-primary me-2"></i>{{ supplier.name }}
            </h3>
            <p class="text-muted mb-0">
                <i class="fas fa-phone me-1"></i>{{ supplier.phone_number }}
                {% if supplier.notes %} • {{ supplier.notes }}{% endif %}
            </p>
        </div>
        <div class="btn-group">
            {% if can_edit %}
            <a href="{% url 'inventory:supplier_update' supplier.id %}" class="btn btn-outline-primary">
                <i class="fas fa-edit"></i>
            </a>
            {% endif %}
            <a href="{% url 'inventory:supplier_list' %}" class="btn btn-light">
                <i class="fas fa-arrow-left"></i>
            </a>
        </div>
    </div>

    <!-- 💰 QARZ STATISTIKA -->
    <div class="row g-3 mb-4">
        <div class="col-md-3">
            <div class="stat-card border-0 shadow-sm">
                <div class="stat-icon bg-info">
                    <i class="fas fa-history"></i>
                </div>
                <div class="stat-content">
                    <small class="text-muted">Boshlang'ich</small>
                    <h4>${{ supplier.initial_debt|floatformat:2|intcomma }}</h4>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="stat-card border-0 shadow-sm">
                <div class="stat-icon bg-warning">
                    <i class="fas fa-mobile-alt"></i>
                </div>
                <div class="stat-content">
                    <small class="text-muted">Telefonlar</small>
                    <h4>${{ supplier.total_debt|floatformat:2|intcomma }}</h4>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="stat-card border-0 shadow-sm">
                <div class="stat-icon bg-success">
                    <i class="fas fa-check"></i>
                </div>
                <div class="stat-content">
                    <small class="text-muted">To'langan</small>
                    <h4>${{ supplier.total_paid|floatformat:2|intcomma }}</h4>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="stat-card border-0 shadow-sm border-start border-danger border-4">
                <div class="stat-icon bg-danger">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <div class="stat-content">
                    <small class="text-muted">Jami Qarz</small>
                    <h4 class="text-danger">${{ balance|floatformat:2|intcomma }}</h4>
                </div>
            </div>
        </div>
    </div>

    <!-- 🚨 TO'LOV ALERT -->
    {% if balance > 0 and can_edit %}
    <div class="alert alert-warning d-flex justify-content-between align-items-center mb-4">
        <span>
            <i class="fas fa-exclamation-triangle me-2"></i>
            <strong>Qarz:</strong> ${{ balance|floatformat:2|intcomma }}
        </span>
        <a href="{% url 'inventory:supplier_payment_create' supplier.id %}" class="btn btn-success">
            <i class="fas fa-money-bill-wave me-2"></i>To'lov
        </a>
    </div>
    {% elif balance <= 0 %}
    <div class="alert alert-success mb-4">
        <i class="fas fa-check-circle me-2"></i>Barcha qarzlar to'langan
    </div>
    {% endif %}

    <div class="row g-4">
        <!-- 📱 QARZDA TELEFONLAR -->
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-white border-bottom d-flex justify-content-between">
                    <h6 class="mb-0 fw-semibold">
                        <i class="fas fa-clock text-warning me-2"></i>Qarzda
                    </h6>
                    <span class="badge bg-warning">{{ debt_phones.count }}</span>
                </div>
                <div class="card-body p-0">
                    {% if debt_phones %}
                    <div class="table-responsive" style="max-height:450px; overflow-y:auto;">
                        <table class="table table-sm table-hover mb-0">
                            <thead class="sticky-top bg-light">
                                <tr>
                                    <th>Model</th>
                                    <th>IMEI</th>
                                    <th class="text-end">Narx</th>
                                    <th class="text-end">Qarz</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for phone in debt_phones %}
                                <tr>
                                    <td>
                                        <strong>{{ phone.phone_model }}</strong>
                                        <small class="text-muted d-block">{{ phone.memory_size }}</small>
                                    </td>
                                    <td><code>{{ phone.imei|truncatechars:12 }}</code></td>
                                    <td class="text-end">${{ phone.cost_price|floatformat:2 }}</td>
                                    <td class="text-end text-danger fw-bold">
                                        ${{ phone.debt_balance|floatformat:2 }}
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                            <tfoot class="bg-light">
                                <tr>
                                    <th colspan="3" class="text-end">Jami:</th>
                                    <th class="text-end text-danger">
                                        ${{ supplier.total_debt|floatformat:2 }}
                                    </th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    {% else %}
                    <div class="text-center py-5">
                        <i class="fas fa-check-circle fa-3x text-success mb-3 opacity-25"></i>
                        <p class="text-muted">Qarzda telefon yo'q</p>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- ✅ TO'LANGAN TELEFONLAR -->
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-white border-bottom d-flex justify-content-between">
                    <h6 class="mb-0 fw-semibold">
                        <i class="fas fa-check-circle text-success me-2"></i>To'langan
                    </h6>
                    <span class="badge bg-success">{{ paid_phones_total }}</span>
                </div>
                <div class="card-body p-0">
                    {% if paid_phones %}
                    <div class="table-responsive" style="max-height:450px; overflow-y:auto;">
                        <table class="table table-sm table-hover mb-0">
                            <thead class="sticky-top bg-light">
                                <tr>
                                    <th>Model</th>
                                    <th>IMEI</th>
                                    <th class="text-end">Narx</th>
                                    <th>Sana</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for phone in paid_phones %}
                                <tr>
                                    <td>
                                        <strong>{{ phone.phone_model }}</strong>
                                        <small class="text-muted d-block">{{ phone.memory_size }}</small>
                                    </td>
                                    <td><code>{{ phone.imei|truncatechars:12 }}</code></td>
                                    <td class="text-end text-success fw-bold">
                                        ${{ phone.cost_price|floatformat:2 }}
                                    </td>
                                    <td><small>{{ phone.created_at|date:"d.m.Y" }}</small></td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>

                    <!-- Pagination -->
                    {% if paid_phones.has_other_pages %}
                    <div class="card-footer bg-white border-top py-2">
                        <div class="d-flex justify-content-between align-items-center">
                            <small class="text-muted">
                                {{ paid_phones.start_index }}-{{ paid_phones.end_index }} / {{ paid_phones_total }}
                            </small>
                            <div class="btn-group btn-group-sm">
                                {% if paid_phones.has_previous %}
                                <a href="?page={{ paid_phones.previous_page_number }}" class="btn btn-outline-secondary">
                                    <i class="fas fa-chevron-left"></i>
                                </a>
                                {% endif %}
                                <span class="btn btn-light disabled">{{ paid_phones.number }}</span>
                                {% if paid_phones.has_next %}
                                <a href="?page={{ paid_phones.next_page_number }}" class="btn btn-outline-secondary">
                                    <i class="fas fa-chevron-right"></i>
                                </a>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                    {% endif %}
                    {% else %}
                    <div class="text-center py-5">
                        <i class="fas fa-mobile-alt fa-3x text-muted mb-3 opacity-25"></i>
                        <p class="text-muted">To'langan telefon yo'q</p>
                    </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- 📋 TO'LOVLAR TARIXI -->
    <div class="card border-0 shadow-sm mt-4">
        <div class="card-header bg-white border-bottom d-flex justify-content-between">
            <h6 class="mb-0 fw-semibold">
                <i class="fas fa-history text-primary me-2"></i>To'lovlar
            </h6>
            <span class="badge bg-primary">{{ payments.count }}</span>
        </div>
        <div class="card-body p-0">
            {% if payments %}
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th>Sana</th>
                            <th>Summa</th>
                            <th>Turi</th>
                            <th>💵 Manba</th>
                            <th>Tel.</th>
                            <th>Kim</th>
                            <th width="120"></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for payment in payments %}
                        <tr>
                            <td>
                                <strong>{{ payment.payment_date|date:"d.m.Y" }}</strong>
                                <small class="text-muted d-block">{{ payment.created_at|date:"H:i" }}</small>
                            </td>
                            <td>
                                <span class="badge bg-success">
                                    ${{ payment.amount|floatformat:2|intcomma }}
                                </span>
                            </td>
                            <td>
                                <span class="badge bg-{% if payment.payment_type == 'general' %}info{% else %}warning{% endif %}">
                                    {% if payment.payment_type == 'general' %}FIFO{% else %}Maxsus{% endif %}
                                </span>
                            </td>
                            <td>
                                {% if payment.payment_source == 'cash' %}
                                    <span class="badge bg-danger">💵 Kassa</span>
                                    <small class="text-danger d-block">
                                        <i class="fas fa-exclamation-triangle"></i> Chiqim
                                    </small>
                                {% else %}
                                    <span class="badge bg-secondary">🔒 Seyf</span>
                                {% endif %}
                            </td>
                            <td>
                                <span class="badge bg-secondary">{{ payment.details.count }}</span>
                            </td>
                            <td>
                                <small>{{ payment.created_by.username }}</small>
                            </td>
                            <td>
                                <div class="btn-group btn-group-sm">
                                    <a href="{% url 'inventory:supplier_payment_detail' payment.id %}"
                                       class="btn btn-outline-primary" title="Ko'rish">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    {% if can_edit %}
                                    <a href="{% url 'inventory:supplier_payment_update' payment.id %}"
                                       class="btn btn-outline-warning" title="Tahrirlash">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a href="{% url 'inventory:supplier_payment_delete' payment.id %}"
                                       class="btn btn-outline-danger" title="O'chirish">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                    {% endif %}
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            {% else %}
            <div class="text-center py-5">
                <i class="fas fa-file-invoice-dollar fa-3x text-muted mb-3 opacity-25"></i>
                <p class="text-muted">To'lovlar yo'q</p>
            </div>
            {% endif %}
        </div>
    </div>
</div>

<style>
/* Stat Card */
.stat-card {
    padding: 1.5rem;
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    transition: transform 0.2s;
}

.stat-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 16px rgba(0,0,0,0.1) !important;
}

.stat-icon {
    width: 56px;
    height: 56px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    color: white;
}

.stat-content h4 {
    margin: 0;
    font-size: 1.5rem;
    font-weight: 600;
}

.stat-content small {
    display: block;
    font-size: 0.875rem;
}

/* Sticky Header */
.sticky-top {
    position: sticky;
    top: 0;
    z-index: 10;
}

/* Table Hover */
.table-hover tbody tr:hover {
    background-color: rgba(13, 110, 253, 0.05);
}

/* Code */
code {
    background-color: #f8f9fa;
    padding: 0.2rem 0.4rem;
    border-radius: 0.25rem;
    font-size: 0.875rem;
}
</style>
{% endblock %}