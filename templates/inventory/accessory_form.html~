{% extends 'base.html' %}

{% block page_title %}
    {% if is_edit %}Aksessuar Tahrirlash{% else %}Yangi Aksessuar{% endif %}
{% endblock %}

{% block content %}
<div class="fade-in">
    {% if messages %}
        {% for message in messages %}
            <div class="alert alert-{{ message.tags }} alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
    {% endif %}

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>
            <i class="fas fa-headphones me-2"></i>
            {% if is_edit %}Aksessuar Tahrirlash{% else %}Yangi Aksessuar{% endif %}
        </h2>
        <a href="{% url 'inventory:accessory_list' %}" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-1"></i>Orqaga
        </a>
    </div>

    <div class="card">
        <div class="card-body p-4">
            <form method="post" enctype="multipart/form-data">
                {% csrf_token %}

                <div class="row g-3">
                    <!-- Do'kon -->
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Do'kon <span class="text-danger">*</span></label>
                        {{ form.shop }}
                        {% if form.shop.errors %}<div class="text-danger small">{{ form.shop.errors.0 }}</div>{% endif %}
                    </div>

                    {% if not is_edit %}
                    <!-- Mavjud Kod (faqat yangi yaratishda) -->
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Mavjud Kod (ixtiyoriy)</label>
                        {{ form.existing_code }}
                        {% if form.existing_code.errors %}<div class="text-danger small">{{ form.existing_code.errors.0 }}</div>{% endif %}
                        <div class="form-text">Mavjud aksessuar ustiga qo'shish uchun kodini kiriting</div>
                    </div>
                    {% else %}
                    <!-- Kod (tahrirlashda) -->
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Kod</label>
                        <input type="text" class="form-control" value="{{ accessory.code }}" disabled>
                        <div class="form-text">Kod o'zgartirib bo'lmaydi</div>
                    </div>
                    {% endif %}

                    <!-- Nomi -->
                    <div class="col-md-{% if is_edit %}6{% else %}12{% endif %}">
                        <label class="form-label fw-bold">Nomi <span class="text-danger">*</span></label>
                        {{ form.name }}
                        {% if form.name.errors %}<div class="text-danger small">{{ form.name.errors.0 }}</div>{% endif %}
                    </div>

                    {% if not is_edit %}
                    <!-- Kod (yangi yaratishda) -->
                    <div class="col-md-6" id="code-field">
                        <label class="form-label fw-bold">Kod (ixtiyoriy)</label>
                        {{ form.code }}
                        {% if form.code.errors %}<div class="text-danger small">{{ form.code.errors.0 }}</div>{% endif %}
                        <div class="form-text">Bo'sh qoldirilsa avtomatik beriladi</div>
                    </div>

                    <!-- Taminotchi -->
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Taminotchi</label>
                        {{ form.supplier }}
                        {% if form.supplier.errors %}<div class="text-danger small">{{ form.supplier.errors.0 }}</div>{% endif %}
                    </div>
                    {% else %}
                    <!-- Taminotchi (tahrirlashda) -->
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Taminotchi</label>
                        {{ form.supplier }}
                        {% if form.supplier.errors %}<div class="text-danger small">{{ form.supplier.errors.0 }}</div>{% endif %}
                    </div>
                    {% endif %}

                    {% if not is_edit %}
                    <!-- YANGI YARATISH - 3 TA MAYDON -->
                    <div class="col-md-4">
                        <label class="form-label fw-bold">
                            Tannarx (so'm) <span class="text-danger">*</span>
                        </label>
                        {{ form.purchase_price }}
                        {% if form.purchase_price.errors %}<div class="text-danger small">{{ form.purchase_price.errors.0 }}</div>{% endif %}
                        <div class="form-text">Mingga yaxlitlanadi</div>
                    </div>

                    <div class="col-md-4">
                        <label class="form-label fw-bold">
                            Soni <span class="text-danger">*</span>
                        </label>
                        {{ form.quantity }}
                        {% if form.quantity.errors %}<div class="text-danger small">{{ form.quantity.errors.0 }}</div>{% endif %}
                    </div>

                    <div class="col-md-4">
                        <label class="form-label fw-bold">
                            Sotish Narxi (so'm) <span class="text-danger">*</span>
                        </label>
                        {{ form.sale_price }}
                        {% if form.sale_price.errors %}<div class="text-danger small">{{ form.sale_price.errors.0 }}</div>{% endif %}
                        <div class="form-text">Mingga yaxlitlanadi</div>
                    </div>

                    {% else %}
                    <!-- TAHRIRLASH - FAQAT SOTISH NARXI -->
                    <div class="col-md-4">
                        <label class="form-label fw-bold">Hozirgi Tannarx</label>
                        <input type="text" class="form-control" value="{{ accessory.purchase_price|floatformat:0 }} so'm" disabled>
                        <div class="form-text text-muted">O'rtacha tannarx</div>
                    </div>

                    <div class="col-md-4">
                        <label class="form-label fw-bold">Hozirgi Soni</label>
                        <input type="text" class="form-control" value="{{ accessory.quantity }} dona" disabled>
                        <div class="form-text text-muted">
                            <a href="{% url 'inventory:accessory_add_quantity' accessory.pk %}" class="text-decoration-none">
                                <i class="fas fa-plus-circle"></i> Qo'shish
                            </a>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <label class="form-label fw-bold">
                            Sotish Narxi (so'm) <span class="text-danger">*</span>
                        </label>
                        {{ form.sale_price }}
                        {% if form.sale_price.errors %}<div class="text-danger small">{{ form.sale_price.errors.0 }}</div>{% endif %}
                        <div class="form-text">Mingga yaxlitlanadi</div>
                    </div>
                    {% endif %}

                    <!-- Rasm -->
                    <div class="col-12">
                        <label class="form-label fw-bold">Rasm</label>
                        {% if is_edit and accessory.image %}
                            <div class="mb-2">
                                <img src="{{ accessory.image.url }}" class="rounded" style="max-height: 120px;" alt="{{ accessory.name }}">
                            </div>
                        {% endif %}
                        {{ form.image }}
                        {% if form.image.errors %}<div class="text-danger small">{{ form.image.errors.0 }}</div>{% endif %}
                    </div>

                    <!-- Tugmalar -->
                    <div class="col-12 mt-4">
                        <hr>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-success">
                                <i class="fas fa-save me-1"></i>
                                {% if is_edit %}Yangilash{% else %}Saqlash{% endif %}
                            </button>
                            <a href="{% url 'inventory:accessory_list' %}" class="btn btn-secondary">
                                <i class="fas fa-times me-1"></i>Bekor Qilish
                            </a>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
// Mavjud kod kiritilganda, yangi kod maydonini yashirish
{% if not is_edit %}
document.addEventListener('DOMContentLoaded', function() {
    const existingCodeInput = document.getElementById('existing_code_input');
    const codeField = document.getElementById('code-field');

    if (existingCodeInput && codeField) {
        existingCodeInput.addEventListener('input', function() {
            if (this.value.trim()) {
                codeField.style.display = 'none';
            } else {
                codeField.style.display = 'block';
            }
        });
    }
});
{% endif %}
</script>
{% endblock %}